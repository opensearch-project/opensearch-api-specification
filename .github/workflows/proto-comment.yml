name: Proto Compatibility Comment

on:
  workflow_run:
    workflows:
      - Proto Compatibility Check
    types:
      - completed

jobs:
  comment:
    runs-on: ubuntu-latest
    if: >
      github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.conclusion == 'success'
    steps:
      - name: Download Proto Report Artifact
        uses: actions/download-artifact@v4
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          name: proto-compatibility-report
          run-id: ${{ github.event.workflow_run.id }}

      - name: Extract PR Number and Parse Report
        shell: bash -eo pipefail {0}
        run: |
          # Get PR number from workflow_run event
          PR_NUMBER="${{ github.event.workflow_run.pull_requests[0].number }}"

          if [ -z "$PR_NUMBER" ]; then
            echo "ERROR: Could not extract PR number"
            exit 1
          fi

          echo "PR_NUMBER=$PR_NUMBER" >> $GITHUB_ENV

          # Read report file content and save to environment variable
          if [ -f "report.md" ]; then
            REPORT_CONTENT=$(cat report.md)
            echo "REPORT_CONTENT<<EOF" >> $GITHUB_ENV
            echo "$REPORT_CONTENT" >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
          else
            echo "WARNING: report.md not found"
          fi

      - name: Find Existing Comment
        uses: peter-evans/find-comment@v3
        id: fc
        with:
          issue-number: ${{ env.PR_NUMBER }}
          comment-author: github-actions[bot]
          body-includes: Proto Compatibility Report

      - name: Create or Update Comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          comment-id: ${{ steps.fc.outputs.comment-id }}
          issue-number: ${{ env.PR_NUMBER }}
          body: |
            ## Proto Compatibility Report

            ${{ env.REPORT_CONTENT }}

            ---
            *Generated by Proto Compatibility Check*
          edit-mode: replace
