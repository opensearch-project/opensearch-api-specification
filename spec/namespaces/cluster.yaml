openapi: 3.1.0
info:
  title: OpenSearch Cluster API
  description: OpenSearch Cluster API.
  version: 1.0.0
paths:
  /_cluster/allocation/explain:
    get:
      operationId: cluster.allocation_explain.0
      x-operation-group: cluster.allocation_explain
      x-version-added: '1.0'
      description: Explains how shards are allocated in the current cluster and provides an explanation for why unassigned shards can't be allocated to a node.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-allocation/
      parameters:
        - $ref: '#/components/parameters/cluster.allocation_explain::query.include_disk_info'
        - $ref: '#/components/parameters/cluster.allocation_explain::query.include_yes_decisions'
      requestBody:
        $ref: '#/components/requestBodies/cluster.allocation_explain'
      responses:
        '200':
          $ref: '#/components/responses/cluster.allocation_explain@200'
    post:
      operationId: cluster.allocation_explain.1
      x-operation-group: cluster.allocation_explain
      x-version-added: '1.0'
      description: Explains how shards are allocated in the current cluster and provides an explanation for why unassigned shards can't be allocated to a node.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-allocation/
      parameters:
        - $ref: '#/components/parameters/cluster.allocation_explain::query.include_disk_info'
        - $ref: '#/components/parameters/cluster.allocation_explain::query.include_yes_decisions'
      requestBody:
        $ref: '#/components/requestBodies/cluster.allocation_explain'
      responses:
        '200':
          $ref: '#/components/responses/cluster.allocation_explain@200'
  /_cluster/decommission/awareness:
    delete:
      operationId: cluster.delete_decommission_awareness.0
      x-operation-group: cluster.delete_decommission_awareness
      x-version-added: '1.0'
      description: Recommissions a decommissioned zone.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-decommission/#example-decommissioning-and-recommissioning-a-zone
      responses:
        '200':
          $ref: '#/components/responses/cluster.delete_decommission_awareness@200'
  /_cluster/decommission/awareness/{awareness_attribute_name}/_status:
    get:
      operationId: cluster.get_decommission_awareness.0
      x-operation-group: cluster.get_decommission_awareness
      x-version-added: '1.0'
      description: Retrieves the decommission status for all zones.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-decommission/#example-getting-zone-decommission-status
      parameters:
        - $ref: '#/components/parameters/cluster.get_decommission_awareness::path.awareness_attribute_name'
      responses:
        '200':
          $ref: '#/components/responses/cluster.get_decommission_awareness@200'
  /_cluster/decommission/awareness/{awareness_attribute_name}/{awareness_attribute_value}:
    put:
      operationId: cluster.put_decommission_awareness.0
      x-operation-group: cluster.put_decommission_awareness
      x-version-added: '1.0'
      description: Decommissions a cluster zone based on awareness. This can greatly benefit multi-zone deployments, where awareness attributes can aid in applying new upgrades to a cluster in a controlled fashion. 
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-decommission/#example-decommissioning-and-recommissioning-a-zone
      parameters:
        - $ref: '#/components/parameters/cluster.put_decommission_awareness::path.awareness_attribute_name'
        - $ref: '#/components/parameters/cluster.put_decommission_awareness::path.awareness_attribute_value'
      responses:
        '200':
          $ref: '#/components/responses/cluster.put_decommission_awareness@200'
  /_cluster/health:
    get:
      operationId: cluster.health.0
      x-operation-group: cluster.health
      x-version-added: '1.0'
      description: Returns basic information about the health of the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-health/
      parameters:
        - $ref: '#/components/parameters/cluster.health::query.awareness_attribute'
        - $ref: '#/components/parameters/cluster.health::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.health::query.expand_wildcards'
        - $ref: '#/components/parameters/cluster.health::query.level'
        - $ref: '#/components/parameters/cluster.health::query.local'
        - $ref: '#/components/parameters/cluster.health::query.master_timeout'
        - $ref: '#/components/parameters/cluster.health::query.timeout'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_active_shards'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_events'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_no_initializing_shards'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_no_relocating_shards'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_nodes'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_status'
      responses:
        '200':
          $ref: '#/components/responses/cluster.health@200'
  /_cluster/health/{index}:
    get:
      operationId: cluster.health.1
      x-operation-group: cluster.health
      x-version-added: '1.0'
      description: Returns basic information about the health of the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-health/
      parameters:
        - $ref: '#/components/parameters/cluster.health::path.index'
        - $ref: '#/components/parameters/cluster.health::query.awareness_attribute'
        - $ref: '#/components/parameters/cluster.health::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.health::query.expand_wildcards'
        - $ref: '#/components/parameters/cluster.health::query.level'
        - $ref: '#/components/parameters/cluster.health::query.local'
        - $ref: '#/components/parameters/cluster.health::query.master_timeout'
        - $ref: '#/components/parameters/cluster.health::query.timeout'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_active_shards'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_events'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_no_initializing_shards'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_no_relocating_shards'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_nodes'
        - $ref: '#/components/parameters/cluster.health::query.wait_for_status'
      responses:
        '200':
          $ref: '#/components/responses/cluster.health@200'
  /_cluster/pending_tasks:
    get:
      operationId: cluster.pending_tasks.0
      x-operation-group: cluster.pending_tasks
      x-version-added: '1.0'
      description: |-
        Returns a list of pending cluster-level tasks, such as index creation, mapping updates,
        or new allocations.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.pending_tasks::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.pending_tasks::query.local'
        - $ref: '#/components/parameters/cluster.pending_tasks::query.master_timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.pending_tasks@200'
  /_cluster/reroute:
    post:
      operationId: cluster.reroute.0
      x-operation-group: cluster.reroute
      x-version-added: '1.0'
      description: Allows to manually change the allocation of individual shards in the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.reroute::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.reroute::query.dry_run'
        - $ref: '#/components/parameters/cluster.reroute::query.explain'
        - $ref: '#/components/parameters/cluster.reroute::query.master_timeout'
        - $ref: '#/components/parameters/cluster.reroute::query.metric'
        - $ref: '#/components/parameters/cluster.reroute::query.retry_failed'
        - $ref: '#/components/parameters/cluster.reroute::query.timeout'
      requestBody:
        $ref: '#/components/requestBodies/cluster.reroute'
      responses:
        '200':
          $ref: '#/components/responses/cluster.reroute@200'
  /_cluster/routing/awareness/weights:
    delete:
      operationId: cluster.delete_weighted_routing.0
      x-operation-group: cluster.delete_weighted_routing
      x-version-added: '1.0'
      description: Delete weighted shard routing weights.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-deleting-weights
      requestBody:
        $ref: '#/components/requestBodies/cluster.delete_weighted_routing'
      responses:
        '200':
          $ref: '#/components/responses/cluster.delete_weighted_routing@200'
  /_cluster/routing/awareness/{attribute}/weights:
    get:
      operationId: cluster.get_weighted_routing.0
      x-operation-group: cluster.get_weighted_routing
      x-version-added: '1.0'
      description: Fetches weighted shard routing weights.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-getting-weights-for-all-zones
      parameters:
        - $ref: '#/components/parameters/cluster.get_weighted_routing::path.attribute'
      responses:
        '200':
          $ref: '#/components/responses/cluster.get_weighted_routing@200'
    put:
      operationId: cluster.put_weighted_routing.0
      x-operation-group: cluster.put_weighted_routing
      x-version-added: '1.0'
      description: Updates weighted shard routing weights.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-weighted-round-robin-search
      parameters:
        - $ref: '#/components/parameters/cluster.put_weighted_routing::path.attribute'
      requestBody:
        $ref: '#/components/requestBodies/cluster.put_weighted_routing'
      responses:
        '200':
          $ref: '#/components/responses/cluster.put_weighted_routing@200'
  /_cluster/settings:
    get:
      operationId: cluster.get_settings.0
      x-operation-group: cluster.get_settings
      x-version-added: '1.0'
      description: Returns cluster settings.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-settings/
      parameters:
        - $ref: '#/components/parameters/cluster.get_settings::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.get_settings::query.flat_settings'
        - $ref: '#/components/parameters/cluster.get_settings::query.include_defaults'
        - $ref: '#/components/parameters/cluster.get_settings::query.master_timeout'
        - $ref: '#/components/parameters/cluster.get_settings::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.get_settings@200'
    put:
      operationId: cluster.put_settings.0
      x-operation-group: cluster.put_settings
      x-version-added: '1.0'
      description: Updates the cluster settings.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-settings/
      parameters:
        - $ref: '#/components/parameters/cluster.put_settings::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.put_settings::query.flat_settings'
        - $ref: '#/components/parameters/cluster.put_settings::query.master_timeout'
        - $ref: '#/components/parameters/cluster.put_settings::query.timeout'
      requestBody:
        $ref: '#/components/requestBodies/cluster.put_settings'
      responses:
        '200':
          $ref: '#/components/responses/cluster.put_settings@200'
  /_cluster/state:
    get:
      operationId: cluster.state.0
      x-operation-group: cluster.state
      x-version-added: '1.0'
      description: Returns comprehensive information about the state of the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.state::query.allow_no_indices'
        - $ref: '#/components/parameters/cluster.state::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.state::query.expand_wildcards'
        - $ref: '#/components/parameters/cluster.state::query.flat_settings'
        - $ref: '#/components/parameters/cluster.state::query.ignore_unavailable'
        - $ref: '#/components/parameters/cluster.state::query.local'
        - $ref: '#/components/parameters/cluster.state::query.master_timeout'
        - $ref: '#/components/parameters/cluster.state::query.wait_for_metadata_version'
        - $ref: '#/components/parameters/cluster.state::query.wait_for_timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.state@200'
  /_cluster/state/{metric}:
    get:
      operationId: cluster.state.1
      x-operation-group: cluster.state
      x-version-added: '1.0'
      description: Returns comprehensive information about the state of the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.state::path.metric'
        - $ref: '#/components/parameters/cluster.state::query.allow_no_indices'
        - $ref: '#/components/parameters/cluster.state::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.state::query.expand_wildcards'
        - $ref: '#/components/parameters/cluster.state::query.flat_settings'
        - $ref: '#/components/parameters/cluster.state::query.ignore_unavailable'
        - $ref: '#/components/parameters/cluster.state::query.local'
        - $ref: '#/components/parameters/cluster.state::query.master_timeout'
        - $ref: '#/components/parameters/cluster.state::query.wait_for_metadata_version'
        - $ref: '#/components/parameters/cluster.state::query.wait_for_timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.state@200'
  /_cluster/state/{metric}/{index}:
    get:
      operationId: cluster.state.2
      x-operation-group: cluster.state
      x-version-added: '1.0'
      description: Returns comprehensive information about the state of the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.state::path.index'
        - $ref: '#/components/parameters/cluster.state::path.metric'
        - $ref: '#/components/parameters/cluster.state::query.allow_no_indices'
        - $ref: '#/components/parameters/cluster.state::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.state::query.expand_wildcards'
        - $ref: '#/components/parameters/cluster.state::query.flat_settings'
        - $ref: '#/components/parameters/cluster.state::query.ignore_unavailable'
        - $ref: '#/components/parameters/cluster.state::query.local'
        - $ref: '#/components/parameters/cluster.state::query.master_timeout'
        - $ref: '#/components/parameters/cluster.state::query.wait_for_metadata_version'
        - $ref: '#/components/parameters/cluster.state::query.wait_for_timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.state@200'
  /_cluster/stats:
    get:
      operationId: cluster.stats.0
      x-operation-group: cluster.stats
      x-version-added: '1.0'
      description: Returns a high-level overview of cluster statistics.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/
      parameters:
        - $ref: '#/components/parameters/cluster.stats::query.flat_settings'
        - $ref: '#/components/parameters/cluster.stats::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.stats@200'
  /_cluster/stats/nodes/{node_id}:
    get:
      operationId: cluster.stats.1
      x-operation-group: cluster.stats
      x-version-added: '1.0'
      description: Returns a high-level overview of cluster statistics.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/
      parameters:
        - $ref: '#/components/parameters/cluster.stats::path.node_id'
        - $ref: '#/components/parameters/cluster.stats::query.flat_settings'
        - $ref: '#/components/parameters/cluster.stats::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.stats@200'
  /_cluster/stats/{metric}/nodes/{node_id}:
    get:
      operationId: cluster.stats.2
      x-operation-group: cluster.stats
      x-version-added: '2.18'
      description: Returns a high-level overview of cluster statistics.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/
      parameters:
        - $ref: '#/components/parameters/cluster.stats::path.metric'
        - $ref: '#/components/parameters/cluster.stats::path.node_id'
        - $ref: '#/components/parameters/cluster.stats::query.flat_settings'
        - $ref: '#/components/parameters/cluster.stats::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.stats@200'
  /_cluster/stats/{metric}/{index_metric}/nodes/{node_id}:
    get:
      operationId: cluster.stats.3
      x-operation-group: cluster.stats
      x-version-added: '2.18'
      description: Returns a high-level overview of cluster statistics.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/
      parameters:
        - $ref: '#/components/parameters/cluster.stats::path.index_metric'
        - $ref: '#/components/parameters/cluster.stats::path.metric'
        - $ref: '#/components/parameters/cluster.stats::path.node_id'
        - $ref: '#/components/parameters/cluster.stats::query.flat_settings'
        - $ref: '#/components/parameters/cluster.stats::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.stats@200'
  /_cluster/voting_config_exclusions:
    post:
      operationId: cluster.post_voting_config_exclusions.0
      x-operation-group: cluster.post_voting_config_exclusions
      x-version-added: '1.0'
      description: Updates the cluster voting configuration by excluding certain node IDs or names.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.post_voting_config_exclusions::query.node_ids'
        - $ref: '#/components/parameters/cluster.post_voting_config_exclusions::query.node_names'
        - $ref: '#/components/parameters/cluster.post_voting_config_exclusions::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.post_voting_config_exclusions@200'
    delete:
      operationId: cluster.delete_voting_config_exclusions.0
      x-operation-group: cluster.delete_voting_config_exclusions
      x-version-added: '1.0'
      description: Clears any cluster voting configuration exclusions.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.delete_voting_config_exclusions::query.wait_for_removal'
      responses:
        '200':
          $ref: '#/components/responses/cluster.delete_voting_config_exclusions@200'
  /_component_template:
    get:
      operationId: cluster.get_component_template.0
      x-operation-group: cluster.get_component_template
      x-version-added: '1.0'
      description: Returns one or more component templates.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.get_component_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.get_component_template::query.flat_settings'
        - $ref: '#/components/parameters/cluster.get_component_template::query.local'
        - $ref: '#/components/parameters/cluster.get_component_template::query.master_timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.get_component_template@200'
  /_component_template/{name}:
    get:
      operationId: cluster.get_component_template.1
      x-operation-group: cluster.get_component_template
      x-version-added: '1.0'
      description: Returns one or more component templates.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.get_component_template::path.name'
        - $ref: '#/components/parameters/cluster.get_component_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.get_component_template::query.flat_settings'
        - $ref: '#/components/parameters/cluster.get_component_template::query.local'
        - $ref: '#/components/parameters/cluster.get_component_template::query.master_timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.get_component_template@200'
    head:
      operationId: cluster.exists_component_template.0
      x-operation-group: cluster.exists_component_template
      x-version-added: '1.0'
      description: Returns information about whether a particular component template exist.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.exists_component_template::path.name'
        - $ref: '#/components/parameters/cluster.exists_component_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.exists_component_template::query.local'
        - $ref: '#/components/parameters/cluster.exists_component_template::query.master_timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.exists_component_template@200'
    post:
      operationId: cluster.put_component_template.0
      x-operation-group: cluster.put_component_template
      x-version-added: '1.0'
      description: Creates or updates a component template.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-templates/#use-component-templates-to-create-an-index-template
      parameters:
        - $ref: '#/components/parameters/cluster.put_component_template::path.name'
        - $ref: '#/components/parameters/cluster.put_component_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.put_component_template::query.create'
        - $ref: '#/components/parameters/cluster.put_component_template::query.master_timeout'
        - $ref: '#/components/parameters/cluster.put_component_template::query.timeout'
      requestBody:
        $ref: '#/components/requestBodies/cluster.put_component_template'
      responses:
        '200':
          $ref: '#/components/responses/cluster.put_component_template@200'
    put:
      operationId: cluster.put_component_template.1
      x-operation-group: cluster.put_component_template
      x-version-added: '1.0'
      description: Creates or updates a component template.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-templates/#use-component-templates-to-create-an-index-template
      parameters:
        - $ref: '#/components/parameters/cluster.put_component_template::path.name'
        - $ref: '#/components/parameters/cluster.put_component_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.put_component_template::query.create'
        - $ref: '#/components/parameters/cluster.put_component_template::query.master_timeout'
        - $ref: '#/components/parameters/cluster.put_component_template::query.timeout'
      requestBody:
        $ref: '#/components/requestBodies/cluster.put_component_template'
      responses:
        '200':
          $ref: '#/components/responses/cluster.put_component_template@200'
    delete:
      operationId: cluster.delete_component_template.0
      x-operation-group: cluster.delete_component_template
      x-version-added: '1.0'
      description: Deletes a component template.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/cluster.delete_component_template::path.name'
        - $ref: '#/components/parameters/cluster.delete_component_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cluster.delete_component_template::query.master_timeout'
        - $ref: '#/components/parameters/cluster.delete_component_template::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cluster.delete_component_template@200'
  /_remote/info:
    get:
      operationId: cluster.remote_info.0
      x-operation-group: cluster.remote_info
      x-version-added: '1.0'
      description: Returns the information about configured remote clusters.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/remote-info/
      parameters: []
      responses:
        '200':
          $ref: '#/components/responses/cluster.remote_info@200'
components:
  requestBodies:
    cluster.put_weighted_routing:
      content:
        application/json:
          schema:
            $ref: '../schemas/cluster.weighted_routing.yaml#/components/schemas/Weights'
    cluster.delete_weighted_routing:
      content:
        application/json:
          schema:
            $ref: '../schemas/cluster.weighted_routing.yaml#/components/schemas/WeightsBase'
    cluster.allocation_explain:
      content:
        application/json:
          schema:
            type: object
            properties:
              current_node:
                description: Specifies the node ID or the name of the node to only explain a shard that is currently located on the specified node.
                type: string
              index:
                $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
              primary:
                description: When `true`, returns a routing explanation for the primary shard based on the node ID.
                type: boolean
              shard:
                description: Specifies the ID of the shard that you would like an explanation for.
                type: integer
                format: int32
            description: The index, shard, and primary flag for which to generate an explanation. Leave this empty to generate an explanation for the first unassigned shard.
    cluster.put_component_template:
      content:
        application/json:
          schema:
            type: object
            properties:
              allow_auto_create:
                description: |-
                  This setting overrides the `action.auto_create_index` cluster setting.
                  When set to `true` in a component template, indexes are automatically created using that
                  template. When set to `false`, data streams matching the component template must always be explicitly created.
                type: boolean
              template:
                $ref: '../schemas/indices._common.yaml#/components/schemas/IndexState'
              version:
                $ref: '../schemas/_common.yaml#/components/schemas/VersionNumber'
              _meta:
                $ref: '../schemas/_common.yaml#/components/schemas/Metadata'
            required:
              - template
            description: The template definition.
      required: true
    cluster.put_settings:
      content:
        application/json:
          schema:
            type: object
            properties:
              persistent:
                type: object
                additionalProperties: {}
              transient:
                type: object
                additionalProperties: {}
            description: The cluster settings to update. 
      required: true
    cluster.reroute:
      content:
        application/json:
          schema:
            type: object
            properties:
              commands:
                description: Defines the reroute commands to perform, either `move`, `cancel`, or `allocate`.
                type: array
                items:
                  $ref: '../schemas/cluster.reroute.yaml#/components/schemas/Command'
            description: The definition of `commands` to perform (`move`, `cancel`, `allocate`)
  responses:
    cluster.allocation_explain@200:
      content:
        application/json:
          schema:
            type: object
            properties:
              allocate_explanation:
                type: string
              allocation_delay:
                $ref: '../schemas/_common.yaml#/components/schemas/Duration'
              allocation_delay_in_millis:
                $ref: '../schemas/_common.yaml#/components/schemas/DurationValueUnitMillis'
              can_allocate:
                $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/Decision'
              can_move_to_other_node:
                $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/Decision'
              can_rebalance_cluster:
                $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/Decision'
              can_rebalance_cluster_decisions:
                type: array
                items:
                  $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/AllocationDecision'
              can_rebalance_to_other_node:
                $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/Decision'
              can_remain_decisions:
                type: array
                items:
                  $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/AllocationDecision'
              can_remain_on_current_node:
                $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/Decision'
              cluster_info:
                $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/ClusterInfo'
              configured_delay:
                $ref: '../schemas/_common.yaml#/components/schemas/Duration'
              configured_delay_in_millis:
                $ref: '../schemas/_common.yaml#/components/schemas/DurationValueUnitMillis'
              current_node:
                $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/CurrentNode'
              current_state:
                type: string
              index:
                $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
              move_explanation:
                type: string
              node_allocation_decisions:
                type: array
                items:
                  $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/NodeAllocationExplanation'
              primary:
                type: boolean
              rebalance_explanation:
                type: string
              remaining_delay:
                $ref: '../schemas/_common.yaml#/components/schemas/Duration'
              remaining_delay_in_millis:
                $ref: '../schemas/_common.yaml#/components/schemas/DurationValueUnitMillis'
              shard:
                type: integer
                format: int32
              unassigned_info:
                $ref: '../schemas/cluster.allocation_explain.yaml#/components/schemas/UnassignedInformation'
              note:
                type: string
            required:
              - current_state
              - index
              - primary
              - shard
    cluster.delete_component_template@200:
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    cluster.delete_decommission_awareness@200:
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    cluster.delete_voting_config_exclusions@200:
      content:
        application/json: {}
    cluster.delete_weighted_routing@200:
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    cluster.exists_component_template@200:
      content:
        application/json: {}
    cluster.get_component_template@200:
      content:
        application/json:
          schema:
            type: object
            properties:
              component_templates:
                type: array
                items:
                  $ref: '../schemas/cluster._common.yaml#/components/schemas/ComponentTemplate'
            required:
              - component_templates
    cluster.get_decommission_awareness@200:
      content:
        application/json:
          schema:
            $ref: '../schemas/cluster.decommission_awareness.yaml#/components/schemas/DecommissionStatusResponse'
    cluster.get_settings@200:
      content:
        application/json:
          schema:
            type: object
            properties:
              persistent:
                type: object
                additionalProperties: true
              transient:
                type: object
                additionalProperties: true
              defaults:
                type: object
                additionalProperties: true
            required:
              - persistent
              - transient
    cluster.get_weighted_routing@200:
      content:
        application/json:
          schema:
            $ref: '../schemas/cluster.weighted_routing.yaml#/components/schemas/WeightsResponse'
    cluster.health@200:
      content:
        application/json:
          schema:
            $ref: '../schemas/cluster.health.yaml#/components/schemas/HealthResponseBody'
    cluster.pending_tasks@200:
      content:
        application/json:
          schema:
            type: object
            properties:
              tasks:
                type: array
                items:
                  $ref: '../schemas/cluster.pending_tasks.yaml#/components/schemas/PendingTask'
            required:
              - tasks
    cluster.post_voting_config_exclusions@200:
      content:
        application/json: {}
    cluster.put_component_template@200:
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    cluster.put_decommission_awareness@200:
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    cluster.put_settings@200:
      content:
        application/json:
          schema:
            type: object
            properties:
              acknowledged:
                type: boolean
              persistent:
                type: object
                additionalProperties: {}
              transient:
                type: object
                additionalProperties: {}
            required:
              - acknowledged
              - persistent
              - transient
    cluster.put_weighted_routing@200:
      content:
        application/json:
          schema:
            type: object
    cluster.remote_info@200:
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              $ref: '../schemas/cluster.remote_info.yaml#/components/schemas/ClusterRemoteInfo'
    cluster.reroute@200:
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
              - type: object
                properties:
                  explanations:
                    type: array
                    items:
                      $ref: '../schemas/cluster.reroute.yaml#/components/schemas/RerouteExplanation'
                  state:
                    description: |-
                      Shows the internal representation of the structure, which can
                      differ from the external representation.                  
    cluster.state@200:
      content:
        application/json:
          schema: {}
    cluster.stats@200:
      content:
        application/json:
          schema:
            allOf:
              - $ref: '../schemas/nodes._common.yaml#/components/schemas/NodesResponseBase'
              - type: object
                properties:
                  cluster_name:
                    description: The name of the cluster.
                    $ref: '../schemas/_common.yaml#/components/schemas/Name'
                  cluster_uuid:
                    description: The unique identifier of the cluster.
                    $ref: '../schemas/_common.yaml#/components/schemas/Uuid'
                  indices:
                    description: Displays statistics about indexes in the cluster.
                    $ref: '../schemas/cluster.stats.yaml#/components/schemas/ClusterIndices'
                  nodes:
                    description: Displays statistics about nodes selected by the request's node filters.
                    $ref: '../schemas/cluster.stats.yaml#/components/schemas/ClusterNodes'
                  status:
                    description: The health status of the cluster, based on the state of its primary and replica shards.
                    $ref: '../schemas/_common.yaml#/components/schemas/HealthStatus'
                  timestamp:
                    description: The Unix timestamp, in milliseconds, indicating when any cluster statistics were last refreshed.
                    $ref: '../schemas/_common.yaml#/components/schemas/EpochTimeUnitMillis'
                required:
                  - cluster_name
                  - cluster_uuid
                  - status
                  - timestamp
  parameters:
    cluster.allocation_explain::query.include_disk_info:
      in: query
      name: include_disk_info
      description: When `true`, returns information about disk usage and shard sizes.
      schema:
        type: boolean
        default: false
      style: form
    cluster.allocation_explain::query.include_yes_decisions:
      in: query
      name: include_yes_decisions
      description: When `true`, returns any `YES` decisions in the allocation explanation. `YES` decisions indicate when a particular shard allocation attempt was successful for the given node.
      schema:
        type: boolean
        default: false
      style: form
    cluster.delete_component_template::path.name:
      in: path
      name: name
      description: |-
        The name of the component template to delete.
        Supports wildcard (*) expressions.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    cluster.delete_component_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.delete_component_template::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.delete_component_template::query.timeout:
      in: query
      name: timeout
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    cluster.delete_voting_config_exclusions::query.wait_for_removal:
      in: query
      name: wait_for_removal
      description: |-
        Specifies whether to wait for all excluded nodes to be removed from the
        cluster before clearing the voting configuration exclusions list.
        When `true`, all excluded nodes are removed from
        the cluster before this API takes any action. When `false`, the
        voting configuration exclusions list is cleared even if some excluded
        nodes are still in the cluster.
      schema:
        type: boolean
        default: true
      style: form
    cluster.exists_component_template::path.name:
      in: path
      name: name
      description: |-
        The name of the component template.
        Wildcard (*) expressions are supported.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    cluster.exists_component_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      in: query
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.exists_component_template::query.local:
      in: query
      name: local
      description: |-
        When `true`, the request retrieves information from the local node only.
        When `false, information is retrieved from the cluster manager node.
      schema:
        type: boolean
        default: false
      style: form
    cluster.exists_component_template::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.get_component_template::path.name:
      in: path
      name: name
      description: |-
        The name of the component template to retrieve.
        Wildcard (`*`) expressions are supported.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    cluster.get_component_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      in: query
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
        default: 30s
      x-version-added: '2.0'
    cluster.get_component_template::query.flat_settings:
      in: query
      name: flat_settings
      description: |- 
        Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings.
        For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`.
      schema:
        type: boolean
        default: false
      style: form
    cluster.get_component_template::query.local:
      in: query
      name: local
      description: |-
        When `true`, the request retrieves information from the local node only.
        When `false`, information is retrieved from the cluster manager node.
      schema:
        type: boolean
        default: false
      style: form
    cluster.get_component_template::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.get_decommission_awareness::path.awareness_attribute_name:
      name: awareness_attribute_name
      in: path
      description: The name of the awareness attribute.
      schema:
        type: string
        description: The name of the awareness attribute.
      required: true
    cluster.get_settings::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      in: query
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.get_settings::query.flat_settings:
      in: query
      name: flat_settings
      description: |- 
        Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings.
        For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`.
      schema:
        type: boolean
        default: false
      style: form
    cluster.get_settings::query.include_defaults:
      in: query
      name: include_defaults
      description: When `true`, returns default cluster settings from the local node.
      schema:
        type: boolean
        default: false
      style: form
    cluster.get_settings::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.get_settings::query.timeout:
      in: query
      name: timeout
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    cluster.get_weighted_routing::path.attribute:
      name: attribute
      in: path
      description: The name of the awareness attribute.
      schema:
        type: string
        description: The name of the awareness attribute.
      required: true
    cluster.health::path.index:
      in: path
      name: index
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    cluster.health::query.awareness_attribute:
      name: awareness_attribute
      in: query
      description: The name of the awareness attribute for which to return the cluster health status (for example, `zone`). Applicable only if `level` is set to `awareness_attributes`.
      schema:
        type: string
        description: The name of the awareness attribute for which to return the cluster health status (for example, `zone`). Applicable only if `level` is set to `awareness_attributes`.
    cluster.health::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.health::query.expand_wildcards:
      in: query
      name: expand_wildcards
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
        default: open
      style: form
    cluster.health::query.level:
      in: query
      name: level
      schema:
        $ref: '../schemas/cluster.health.yaml#/components/schemas/Level'
        default: cluster
      style: form
    cluster.health::query.local:
      in: query
      name: local
      description: Whether to return information from the local node only instead of from the cluster manager node.
      schema:
        type: boolean
        default: false
      style: form
    cluster.health::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.health::query.timeout:
      in: query
      name: timeout
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    cluster.health::query.wait_for_active_shards:
      in: query
      name: wait_for_active_shards
      schema:
        default: 0
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForActiveShards'
      style: form
    cluster.health::query.wait_for_events:
      in: query
      name: wait_for_events
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForEvents'
      style: form
    cluster.health::query.wait_for_no_initializing_shards:
      in: query
      name: wait_for_no_initializing_shards
      description: Whether to wait until there are no initializing shards in the cluster.
      schema:
        type: boolean
        default: false
      style: form
    cluster.health::query.wait_for_no_relocating_shards:
      in: query
      name: wait_for_no_relocating_shards
      description: Whether to wait until there are no relocating shards in the cluster.
      schema:
        type: boolean
        default: false
      style: form
    cluster.health::query.wait_for_nodes:
      in: query
      name: wait_for_nodes
      description: Waits until the specified number of nodes (`N`) is available. Accepts `>=N`, `<=N`, `>N`, and `<N`. You can also use `ge(N)`, `le(N)`, `gt(N)`, and `lt(N)` notation.
      schema:
        oneOf:
          - type: string
          - type: number
      style: form
    cluster.health::query.wait_for_status:
      in: query
      name: wait_for_status
      description: Waits until the cluster health reaches the specified status or better.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/HealthStatus'
      style: form
    cluster.pending_tasks::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      in: query
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.pending_tasks::query.local:
      in: query
      name: local
      description: |-
        When `true`, the request retrieves information from the local node only.
        When `false`, information is retrieved from the cluster manager node.
      schema:
        type: boolean
        default: false
      style: form
    cluster.pending_tasks::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.post_voting_config_exclusions::query.node_ids:
      in: query
      name: node_ids
      description: |-
        A comma-separated list of node IDs to exclude
        from the voting configuration. When using this setting, you cannot also specify `node_names`. Either `node_ids` or `node_names` are required to receive a valid response.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Ids'
      style: form
    cluster.post_voting_config_exclusions::query.node_names:
      in: query
      name: node_names
      description: |-
        A comma-separated list of node names to exclude from the
        voting configuration. When using this setting, you cannot also specify `node_ids`. Either `node_ids` or `node_names` are required to receive a valid response.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: form
    cluster.post_voting_config_exclusions::query.timeout:
      in: query
      name: timeout
      description: |-
        When adding a voting configuration exclusion, the API waits for the
        specified nodes to be excluded from the voting configuration before
        returning a response. If the timeout expires before the appropriate condition
        is satisfied, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    cluster.put_component_template::path.name:
      in: path
      name: name
      description: |-
        The name of the component template to create.
        OpenSearch includes the following built-in component templates: `logs-mappings`, `logs-settings`, `metrics-mappings`, `metrics-settings`, `synthetics-mapping`, and `synthetics-settings`.
        OpenSearch uses these templates to configure backing indexes for its data streams.
        If you want to overwrite one of these templates, set the replacement template `version` to a higher value than the current version.
        If you want to disable all built-in component and index templates, set `stack.templates.enabled` to `false` using the [Cluster Update Settings API](https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-settings/).
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    cluster.put_component_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      in: query
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.put_component_template::query.create:
      in: query
      name: create
      description: When `true`, this request cannot replace or update existing component templates.
      schema:
        type: boolean
        default: false
      style: form
    cluster.put_component_template::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.put_component_template::query.timeout:
      name: timeout
      in: query
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
    cluster.put_decommission_awareness::path.awareness_attribute_name:
      name: awareness_attribute_name
      in: path
      description: The name of the awareness attribute.
      schema:
        type: string
        description: The name of the awareness attribute.
      required: true
    cluster.put_decommission_awareness::path.awareness_attribute_value:
      name: awareness_attribute_value
      in: path
      description: The value of the awareness attribute. For example, if you have shards allocated in two different zones, you can give each zone a value of `zone-a` or `zoneb`. The cluster decommission operation decommissions the zone listed in the method.
      schema:
        type: string
        description: The value of the awareness attribute. For example, if you have shards allocated in two different zones, you can give each zone a value of `zone-a` or `zoneb`. The cluster decommission operation decommissions the zone listed in the method.
      required: true
    cluster.put_settings::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      in: query
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.put_settings::query.flat_settings:
      in: query
      name: flat_settings
      description: |- 
        Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings.
        For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`.
      schema:
        type: boolean
        default: false
      style: form
    cluster.put_settings::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.put_settings::query.timeout:
      in: query
      name: timeout
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    cluster.put_weighted_routing::path.attribute:
      name: attribute
      in: path
      description: The name of awareness attribute, usually `zone`.
      schema:
        type: string
        description: The name of awareness attribute, usually `zone`.
      required: true
    cluster.reroute::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.reroute::query.dry_run:
      in: query
      name: dry_run
      description: When `true`, the request simulates the operation and returns the resulting state.
      schema:
        type: boolean
      style: form
    cluster.reroute::query.explain:
      in: query
      name: explain
      description: When `true`, the response contains an explanation of why reroute certain commands can or cannot be executed.
      schema:
        type: boolean
      style: form
    cluster.reroute::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.reroute::query.metric:
      in: query
      name: metric
      description: Limits the information returned to the specified metrics.
      schema:
        oneOf:
          - $ref: '../schemas/cluster.reroute.yaml#/components/schemas/Metric'
          - type: array
            items:
              $ref: '../schemas/cluster.reroute.yaml#/components/schemas/Metric'
      style: form
    cluster.reroute::query.retry_failed:
      in: query
      name: retry_failed
      description: When `true`, retries shard allocation if it was blocked because of too many subsequent failures.
      schema:
        type: boolean
      style: form
    cluster.reroute::query.timeout:
      in: query
      name: timeout
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    cluster.state::path.index:
      in: path
      name: index
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    cluster.state::path.metric:
      in: path
      name: metric
      description: Limits the information returned to only the [specified metric groups](https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/#metric-groups).
      required: true
      schema:
        type: array
        items:
          $ref: '../schemas/cluster.state.yaml#/components/schemas/Metric'
      style: simple
    cluster.state::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: Whether to ignore a wildcard index expression that resolves into no concrete indexes. This includes the `_all` string or when no indexes have been specified.
      schema:
        type: boolean
      style: form
    cluster.state::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      description: The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
      in: query
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: '2.0'
    cluster.state::query.expand_wildcards:
      in: query
      name: expand_wildcards
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    cluster.state::query.flat_settings:
      in: query
      name: flat_settings
      description: |- 
        Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings.
        For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`.
      schema:
        type: boolean
        default: false
      style: form
    cluster.state::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: Whether the specified concrete indexes should be ignored when unavailable (missing or closed).
      schema:
        type: boolean
      style: form
    cluster.state::query.local:
      in: query
      name: local
      description: Whether to return information from the local node only instead of from the cluster manager node.
      schema:
        type: boolean
        default: false
      style: form
    cluster.state::query.master_timeout:
      in: query
      name: master_timeout
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: '2.0'
      x-deprecation-message: To promote inclusive language, use `cluster_manager_timeout` instead.
    cluster.state::query.wait_for_metadata_version:
      in: query
      name: wait_for_metadata_version
      description: Wait for the metadata version to be equal or greater than the specified metadata version.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/VersionNumber'
      style: form
    cluster.state::query.wait_for_timeout:
      in: query
      name: wait_for_timeout
      description: The maximum time to wait for `wait_for_metadata_version` before timing out.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    cluster.stats::path.index_metric:
      in: path
      name: index_metric
      description: A comma-separated list of [index metric groups](https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/#index-metric-groups), for example, `docs,store`.
      required: true
      schema:
        type: array
        items:
          $ref: '../schemas/cluster.stats.yaml#/components/schemas/IndexMetric'
      style: simple
    cluster.stats::path.metric:
      in: path
      name: metric
      description: Limit the information returned to the specified metrics.
      required: true
      schema:
        type: array
        items:
          $ref: '../schemas/cluster.stats.yaml#/components/schemas/Metric'
      style:
        simple
    cluster.stats::path.node_id:
      in: path
      name: node_id
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/NodeIds'
      style: simple
    cluster.stats::query.flat_settings:
      in: query
      name: flat_settings
      description: |- 
        Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings.
        For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`.
      schema:
        type: boolean
        default: false
      style: form
    cluster.stats::query.timeout:
      in: query
      name: timeout
      description: |-
        The amount of time to wait for each node to respond.
        If a node does not respond before its timeout expires, the response does not include its stats.
        However, timed out nodes are included in the response's `_nodes.failed` property. Defaults to no timeout.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
