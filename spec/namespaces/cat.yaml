openapi: 3.1.0
info:
  title: OpenSearch Cat API
  description: OpenSearch Cat API
  version: 1.0.0
paths:
  /_cat:
    get:
      operationId: cat.help.0
      x-operation-group: cat.help
      x-version-added: '1.0'
      description: Returns help for the Cat APIs.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/index/
      parameters:
        - $ref: '#/components/parameters/cat.help::query.help'
        - $ref: '#/components/parameters/cat.help::query.s'
      responses:
        '200':
          $ref: '#/components/responses/cat.help@200'
  /_cat/aliases:
    get:
      operationId: cat.aliases.0
      x-operation-group: cat.aliases
      x-version-added: '1.0'
      description: Shows information about currently configured aliases to indices including filter and routing infos.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-aliases/
      parameters:
        - $ref: '#/components/parameters/cat.aliases::query.format'
        - $ref: '#/components/parameters/cat.aliases::query.local'
        - $ref: '#/components/parameters/cat.aliases::query.h'
        - $ref: '#/components/parameters/cat.aliases::query.help'
        - $ref: '#/components/parameters/cat.aliases::query.s'
        - $ref: '#/components/parameters/cat.aliases::query.v'
        - $ref: '#/components/parameters/cat.aliases::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/cat.aliases@200'
  /_cat/aliases/{name}:
    get:
      operationId: cat.aliases.1
      x-operation-group: cat.aliases
      x-version-added: '1.0'
      description: Shows information about currently configured aliases to indices including filter and routing infos.
      parameters:
        - $ref: '#/components/parameters/cat.aliases::path.name'
        - $ref: '#/components/parameters/cat.aliases::query.format'
        - $ref: '#/components/parameters/cat.aliases::query.local'
        - $ref: '#/components/parameters/cat.aliases::query.h'
        - $ref: '#/components/parameters/cat.aliases::query.help'
        - $ref: '#/components/parameters/cat.aliases::query.s'
        - $ref: '#/components/parameters/cat.aliases::query.v'
        - $ref: '#/components/parameters/cat.aliases::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/cat.aliases@200'
  /_cat/allocation:
    get:
      operationId: cat.allocation.0
      x-operation-group: cat.allocation
      x-version-added: '1.0'
      description: Provides a snapshot of how many shards are allocated to each data node and how much disk space they are using.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-allocation/
      parameters:
        - $ref: '#/components/parameters/cat.allocation::query.format'
        - $ref: '#/components/parameters/cat.allocation::query.bytes'
        - $ref: '#/components/parameters/cat.allocation::query.local'
        - $ref: '#/components/parameters/cat.allocation::query.master_timeout'
        - $ref: '#/components/parameters/cat.allocation::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.allocation::query.h'
        - $ref: '#/components/parameters/cat.allocation::query.help'
        - $ref: '#/components/parameters/cat.allocation::query.s'
        - $ref: '#/components/parameters/cat.allocation::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.allocation@200'
  /_cat/allocation/{node_id}:
    get:
      operationId: cat.allocation.1
      x-operation-group: cat.allocation
      x-version-added: '1.0'
      description: Provides a snapshot of how many shards are allocated to each data node and how much disk space they are using.
      parameters:
        - $ref: '#/components/parameters/cat.allocation::path.node_id'
        - $ref: '#/components/parameters/cat.allocation::query.format'
        - $ref: '#/components/parameters/cat.allocation::query.bytes'
        - $ref: '#/components/parameters/cat.allocation::query.local'
        - $ref: '#/components/parameters/cat.allocation::query.master_timeout'
        - $ref: '#/components/parameters/cat.allocation::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.allocation::query.h'
        - $ref: '#/components/parameters/cat.allocation::query.help'
        - $ref: '#/components/parameters/cat.allocation::query.s'
        - $ref: '#/components/parameters/cat.allocation::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.allocation@200'
  /_cat/cluster_manager:
    get:
      operationId: cat.cluster_manager.0
      x-operation-group: cat.cluster_manager
      x-version-added: '2.0'
      description: Returns information about the cluster-manager node.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-cluster_manager/
      parameters:
        - $ref: '#/components/parameters/cat.cluster_manager::query.format'
        - $ref: '#/components/parameters/cat.cluster_manager::query.local'
        - $ref: '#/components/parameters/cat.cluster_manager::query.master_timeout'
        - $ref: '#/components/parameters/cat.cluster_manager::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.cluster_manager::query.h'
        - $ref: '#/components/parameters/cat.cluster_manager::query.help'
        - $ref: '#/components/parameters/cat.cluster_manager::query.s'
        - $ref: '#/components/parameters/cat.cluster_manager::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.cluster_manager@200'
  /_cat/count:
    get:
      operationId: cat.count.0
      x-operation-group: cat.count
      x-version-added: '1.0'
      description: Provides quick access to the document count of the entire cluster, or individual indices.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-count/
      parameters:
        - $ref: '#/components/parameters/cat.count::query.format'
        - $ref: '#/components/parameters/cat.count::query.h'
        - $ref: '#/components/parameters/cat.count::query.help'
        - $ref: '#/components/parameters/cat.count::query.s'
        - $ref: '#/components/parameters/cat.count::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.count@200'
  /_cat/count/{index}:
    get:
      operationId: cat.count.1
      x-operation-group: cat.count
      x-version-added: '1.0'
      description: Provides quick access to the document count of the entire cluster, or individual indices.
      parameters:
        - $ref: '#/components/parameters/cat.count::path.index'
        - $ref: '#/components/parameters/cat.count::query.format'
        - $ref: '#/components/parameters/cat.count::query.h'
        - $ref: '#/components/parameters/cat.count::query.help'
        - $ref: '#/components/parameters/cat.count::query.s'
        - $ref: '#/components/parameters/cat.count::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.count@200'
  /_cat/fielddata:
    get:
      operationId: cat.fielddata.0
      x-operation-group: cat.fielddata
      x-version-added: '1.0'
      description: Shows how much heap memory is currently being used by fielddata on every data node in the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-field-data/
      parameters:
        - $ref: '#/components/parameters/cat.fielddata::query.format'
        - $ref: '#/components/parameters/cat.fielddata::query.bytes'
        - $ref: '#/components/parameters/cat.fielddata::query.h'
        - $ref: '#/components/parameters/cat.fielddata::query.help'
        - $ref: '#/components/parameters/cat.fielddata::query.s'
        - $ref: '#/components/parameters/cat.fielddata::query.v'
        - $ref: '#/components/parameters/cat.fielddata::query.fields'
      responses:
        '200':
          $ref: '#/components/responses/cat.fielddata@200'
  /_cat/fielddata/{fields}:
    get:
      operationId: cat.fielddata.1
      x-operation-group: cat.fielddata
      x-version-added: '1.0'
      description: Shows how much heap memory is currently being used by fielddata on every data node in the cluster.
      parameters:
        - $ref: '#/components/parameters/cat.fielddata::path.fields'
        - $ref: '#/components/parameters/cat.fielddata::query.format'
        - $ref: '#/components/parameters/cat.fielddata::query.bytes'
        - $ref: '#/components/parameters/cat.fielddata::query.h'
        - $ref: '#/components/parameters/cat.fielddata::query.help'
        - $ref: '#/components/parameters/cat.fielddata::query.s'
        - $ref: '#/components/parameters/cat.fielddata::query.v'
        - $ref: '#/components/parameters/cat.fielddata::query.fields'
      responses:
        '200':
          $ref: '#/components/responses/cat.fielddata@200'
  /_cat/health:
    get:
      operationId: cat.health.0
      x-operation-group: cat.health
      x-version-added: '1.0'
      description: Returns a concise representation of the cluster health.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-health/
      parameters:
        - $ref: '#/components/parameters/cat.health::query.format'
        - $ref: '#/components/parameters/cat.health::query.h'
        - $ref: '#/components/parameters/cat.health::query.help'
        - $ref: '#/components/parameters/cat.health::query.s'
        - $ref: '#/components/parameters/cat.health::query.time'
        - $ref: '#/components/parameters/cat.health::query.ts'
        - $ref: '#/components/parameters/cat.health::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.health@200'
  /_cat/indices:
    get:
      operationId: cat.indices.0
      x-operation-group: cat.indices
      x-version-added: '1.0'
      description: 'Returns information about indices: number of primaries and replicas, document counts, disk size, ...'
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-indices/
      parameters:
        - $ref: '#/components/parameters/cat.indices::query.format'
        - $ref: '#/components/parameters/cat.indices::query.bytes'
        - $ref: '#/components/parameters/cat.indices::query.local'
        - $ref: '#/components/parameters/cat.indices::query.master_timeout'
        - $ref: '#/components/parameters/cat.indices::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.indices::query.h'
        - $ref: '#/components/parameters/cat.indices::query.health'
        - $ref: '#/components/parameters/cat.indices::query.help'
        - $ref: '#/components/parameters/cat.indices::query.pri'
        - $ref: '#/components/parameters/cat.indices::query.s'
        - $ref: '#/components/parameters/cat.indices::query.time'
        - $ref: '#/components/parameters/cat.indices::query.v'
        - $ref: '#/components/parameters/cat.indices::query.include_unloaded_segments'
        - $ref: '#/components/parameters/cat.indices::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/cat.indices@200'
  /_cat/indices/{index}:
    get:
      operationId: cat.indices.1
      x-operation-group: cat.indices
      x-version-added: '1.0'
      description: 'Returns information about indices: number of primaries and replicas, document counts, disk size, ...'
      parameters:
        - $ref: '#/components/parameters/cat.indices::path.index'
        - $ref: '#/components/parameters/cat.indices::query.format'
        - $ref: '#/components/parameters/cat.indices::query.bytes'
        - $ref: '#/components/parameters/cat.indices::query.local'
        - $ref: '#/components/parameters/cat.indices::query.master_timeout'
        - $ref: '#/components/parameters/cat.indices::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.indices::query.h'
        - $ref: '#/components/parameters/cat.indices::query.health'
        - $ref: '#/components/parameters/cat.indices::query.help'
        - $ref: '#/components/parameters/cat.indices::query.pri'
        - $ref: '#/components/parameters/cat.indices::query.s'
        - $ref: '#/components/parameters/cat.indices::query.time'
        - $ref: '#/components/parameters/cat.indices::query.v'
        - $ref: '#/components/parameters/cat.indices::query.include_unloaded_segments'
        - $ref: '#/components/parameters/cat.indices::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/cat.indices@200'
  /_cat/master:
    get:
      operationId: cat.master.0
      x-operation-group: cat.master
      deprecated: true
      x-deprecation-message: To promote inclusive language, please use '/_cat/cluster_manager' instead.
      x-version-added: '1.0'
      x-version-deprecated: '2.0'
      description: Returns information about the cluster-manager node.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-cluster_manager/
      parameters:
        - $ref: '#/components/parameters/cat.master::query.format'
        - $ref: '#/components/parameters/cat.master::query.local'
        - $ref: '#/components/parameters/cat.master::query.master_timeout'
        - $ref: '#/components/parameters/cat.master::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.master::query.h'
        - $ref: '#/components/parameters/cat.master::query.help'
        - $ref: '#/components/parameters/cat.master::query.s'
        - $ref: '#/components/parameters/cat.master::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.master@200'
  /_cat/nodeattrs:
    get:
      operationId: cat.nodeattrs.0
      x-operation-group: cat.nodeattrs
      x-version-added: '1.0'
      description: Returns information about custom node attributes.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-nodeattrs/
      parameters:
        - $ref: '#/components/parameters/cat.nodeattrs::query.format'
        - $ref: '#/components/parameters/cat.nodeattrs::query.local'
        - $ref: '#/components/parameters/cat.nodeattrs::query.master_timeout'
        - $ref: '#/components/parameters/cat.nodeattrs::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.nodeattrs::query.h'
        - $ref: '#/components/parameters/cat.nodeattrs::query.help'
        - $ref: '#/components/parameters/cat.nodeattrs::query.s'
        - $ref: '#/components/parameters/cat.nodeattrs::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.nodeattrs@200'
  /_cat/nodes:
    get:
      operationId: cat.nodes.0
      x-operation-group: cat.nodes
      x-version-added: '1.0'
      description: Returns basic statistics about performance of cluster nodes.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-nodes/
      parameters:
        - $ref: '#/components/parameters/cat.nodes::query.bytes'
        - $ref: '#/components/parameters/cat.nodes::query.format'
        - $ref: '#/components/parameters/cat.nodes::query.full_id'
        - $ref: '#/components/parameters/cat.nodes::query.local'
        - $ref: '#/components/parameters/cat.nodes::query.master_timeout'
        - $ref: '#/components/parameters/cat.nodes::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.nodes::query.h'
        - $ref: '#/components/parameters/cat.nodes::query.help'
        - $ref: '#/components/parameters/cat.nodes::query.s'
        - $ref: '#/components/parameters/cat.nodes::query.time'
        - $ref: '#/components/parameters/cat.nodes::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.nodes@200'
  /_cat/pending_tasks:
    get:
      operationId: cat.pending_tasks.0
      x-operation-group: cat.pending_tasks
      x-version-added: '1.0'
      description: Returns a concise representation of the cluster pending tasks.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-pending-tasks/
      parameters:
        - $ref: '#/components/parameters/cat.pending_tasks::query.format'
        - $ref: '#/components/parameters/cat.pending_tasks::query.local'
        - $ref: '#/components/parameters/cat.pending_tasks::query.master_timeout'
        - $ref: '#/components/parameters/cat.pending_tasks::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.pending_tasks::query.h'
        - $ref: '#/components/parameters/cat.pending_tasks::query.help'
        - $ref: '#/components/parameters/cat.pending_tasks::query.s'
        - $ref: '#/components/parameters/cat.pending_tasks::query.time'
        - $ref: '#/components/parameters/cat.pending_tasks::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.pending_tasks@200'
  /_cat/pit_segments:
    get:
      operationId: cat.pit_segments.0
      x-operation-group: cat.pit_segments
      x-version-added: '2.4'
      description: List segments for one or several PITs.
      externalDocs:
        url: https://opensearch.org/docs/latest/search-plugins/point-in-time-api/
      parameters:
        - $ref: '#/components/parameters/cat.pit_segments::query.format'
        - $ref: '#/components/parameters/cat.pit_segments::query.h'
        - $ref: '#/components/parameters/cat.pit_segments::query.help'
        - $ref: '#/components/parameters/cat.pit_segments::query.s'
        - $ref: '#/components/parameters/cat.pit_segments::query.v'
        - $ref: '#/components/parameters/cat.pit_segments::query.bytes'
      requestBody:
        $ref: '#/components/requestBodies/cat.pit_segments'
      responses:
        '200':
          $ref: '#/components/responses/cat.pit_segments@200'
  /_cat/pit_segments/_all:
    get:
      operationId: cat.all_pit_segments.0
      x-operation-group: cat.all_pit_segments
      x-version-added: '2.4'
      description: Lists all active point-in-time segments.
      externalDocs:
        url: https://opensearch.org/docs/latest/search-plugins/point-in-time-api/
      parameters:
        - $ref: '#/components/parameters/cat.all_pit_segments::query.format'
        - $ref: '#/components/parameters/cat.all_pit_segments::query.h'
        - $ref: '#/components/parameters/cat.all_pit_segments::query.help'
        - $ref: '#/components/parameters/cat.all_pit_segments::query.s'
        - $ref: '#/components/parameters/cat.all_pit_segments::query.v'
        - $ref: '#/components/parameters/cat.all_pit_segments::query.bytes'
      responses:
        '200':
          $ref: '#/components/responses/cat.all_pit_segments@200'
  /_cat/plugins:
    get:
      operationId: cat.plugins.0
      x-operation-group: cat.plugins
      x-version-added: '1.0'
      description: Returns information about installed plugins across nodes node.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-plugins/
      parameters:
        - $ref: '#/components/parameters/cat.plugins::query.format'
        - $ref: '#/components/parameters/cat.plugins::query.local'
        - $ref: '#/components/parameters/cat.plugins::query.master_timeout'
        - $ref: '#/components/parameters/cat.plugins::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.plugins::query.h'
        - $ref: '#/components/parameters/cat.plugins::query.help'
        - $ref: '#/components/parameters/cat.plugins::query.s'
        - $ref: '#/components/parameters/cat.plugins::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.plugins@200'
  /_cat/recovery:
    get:
      operationId: cat.recovery.0
      x-operation-group: cat.recovery
      x-version-added: '1.0'
      description: Returns information about index shard recoveries, both on-going completed.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-plugins/
      parameters:
        - $ref: '#/components/parameters/cat.recovery::query.format'
        - $ref: '#/components/parameters/cat.recovery::query.active_only'
        - $ref: '#/components/parameters/cat.recovery::query.bytes'
        - $ref: '#/components/parameters/cat.recovery::query.detailed'
        - $ref: '#/components/parameters/cat.recovery::query.h'
        - $ref: '#/components/parameters/cat.recovery::query.help'
        - $ref: '#/components/parameters/cat.recovery::query.index'
        - $ref: '#/components/parameters/cat.recovery::query.s'
        - $ref: '#/components/parameters/cat.recovery::query.time'
        - $ref: '#/components/parameters/cat.recovery::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.recovery@200'
  /_cat/recovery/{index}:
    get:
      operationId: cat.recovery.1
      x-operation-group: cat.recovery
      x-version-added: '1.0'
      description: Returns information about index shard recoveries, both on-going completed.
      parameters:
        - $ref: '#/components/parameters/cat.recovery::path.index'
        - $ref: '#/components/parameters/cat.recovery::query.format'
        - $ref: '#/components/parameters/cat.recovery::query.active_only'
        - $ref: '#/components/parameters/cat.recovery::query.bytes'
        - $ref: '#/components/parameters/cat.recovery::query.detailed'
        - $ref: '#/components/parameters/cat.recovery::query.h'
        - $ref: '#/components/parameters/cat.recovery::query.help'
        - $ref: '#/components/parameters/cat.recovery::query.index'
        - $ref: '#/components/parameters/cat.recovery::query.s'
        - $ref: '#/components/parameters/cat.recovery::query.time'
        - $ref: '#/components/parameters/cat.recovery::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.recovery@200'
  /_cat/repositories:
    get:
      operationId: cat.repositories.0
      x-operation-group: cat.repositories
      x-version-added: '1.0'
      description: Returns information about snapshot repositories registered in the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-repositories/
      parameters:
        - $ref: '#/components/parameters/cat.repositories::query.format'
        - $ref: '#/components/parameters/cat.repositories::query.local'
        - $ref: '#/components/parameters/cat.repositories::query.master_timeout'
        - $ref: '#/components/parameters/cat.repositories::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.repositories::query.h'
        - $ref: '#/components/parameters/cat.repositories::query.help'
        - $ref: '#/components/parameters/cat.repositories::query.s'
        - $ref: '#/components/parameters/cat.repositories::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.repositories@200'
  /_cat/segment_replication:
    get:
      operationId: cat.segment_replication.0
      x-operation-group: cat.segment_replication
      x-version-added: 2.6.0
      description: Returns information about both on-going and latest completed Segment Replication events.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-segment-replication/
      parameters:
        - $ref: '#/components/parameters/cat.segment_replication::query.format'
        - $ref: '#/components/parameters/cat.segment_replication::query.h'
        - $ref: '#/components/parameters/cat.segment_replication::query.help'
        - $ref: '#/components/parameters/cat.segment_replication::query.s'
        - $ref: '#/components/parameters/cat.segment_replication::query.v'
        - $ref: '#/components/parameters/cat.segment_replication::query.allow_no_indices'
        - $ref: '#/components/parameters/cat.segment_replication::query.expand_wildcards'
        - $ref: '#/components/parameters/cat.segment_replication::query.ignore_throttled'
        - $ref: '#/components/parameters/cat.segment_replication::query.ignore_unavailable'
        - $ref: '#/components/parameters/cat.segment_replication::query.active_only'
        - $ref: '#/components/parameters/cat.segment_replication::query.completed_only'
        - $ref: '#/components/parameters/cat.segment_replication::query.bytes'
        - $ref: '#/components/parameters/cat.segment_replication::query.detailed'
        - $ref: '#/components/parameters/cat.segment_replication::query.shards'
        - $ref: '#/components/parameters/cat.segment_replication::query.index'
        - $ref: '#/components/parameters/cat.segment_replication::query.time'
        - $ref: '#/components/parameters/cat.segment_replication::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cat.segment_replication@200'
  /_cat/segment_replication/{index}:
    get:
      operationId: cat.segment_replication.1
      x-operation-group: cat.segment_replication
      x-version-added: 2.6.0
      description: Returns information about both on-going and latest completed Segment Replication events.
      parameters:
        - $ref: '#/components/parameters/cat.segment_replication::path.index'
        - $ref: '#/components/parameters/cat.segment_replication::query.format'
        - $ref: '#/components/parameters/cat.segment_replication::query.h'
        - $ref: '#/components/parameters/cat.segment_replication::query.help'
        - $ref: '#/components/parameters/cat.segment_replication::query.s'
        - $ref: '#/components/parameters/cat.segment_replication::query.v'
        - $ref: '#/components/parameters/cat.segment_replication::query.allow_no_indices'
        - $ref: '#/components/parameters/cat.segment_replication::query.expand_wildcards'
        - $ref: '#/components/parameters/cat.segment_replication::query.ignore_throttled'
        - $ref: '#/components/parameters/cat.segment_replication::query.ignore_unavailable'
        - $ref: '#/components/parameters/cat.segment_replication::query.active_only'
        - $ref: '#/components/parameters/cat.segment_replication::query.completed_only'
        - $ref: '#/components/parameters/cat.segment_replication::query.bytes'
        - $ref: '#/components/parameters/cat.segment_replication::query.detailed'
        - $ref: '#/components/parameters/cat.segment_replication::query.shards'
        - $ref: '#/components/parameters/cat.segment_replication::query.index'
        - $ref: '#/components/parameters/cat.segment_replication::query.time'
        - $ref: '#/components/parameters/cat.segment_replication::query.timeout'
      responses:
        '200':
          $ref: '#/components/responses/cat.segment_replication@200'
  /_cat/segments:
    get:
      operationId: cat.segments.0
      x-operation-group: cat.segments
      x-version-added: '1.0'
      description: Provides low-level information about the segments in the shards of an index.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-segments/
      parameters:
        - $ref: '#/components/parameters/cat.segments::query.format'
        - $ref: '#/components/parameters/cat.segments::query.bytes'
        - $ref: '#/components/parameters/cat.segments::query.master_timeout'
        - $ref: '#/components/parameters/cat.segments::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.segments::query.h'
        - $ref: '#/components/parameters/cat.segments::query.help'
        - $ref: '#/components/parameters/cat.segments::query.s'
        - $ref: '#/components/parameters/cat.segments::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.segments@200'
  /_cat/segments/{index}:
    get:
      operationId: cat.segments.1
      x-operation-group: cat.segments
      x-version-added: '1.0'
      description: Provides low-level information about the segments in the shards of an index.
      parameters:
        - $ref: '#/components/parameters/cat.segments::path.index'
        - $ref: '#/components/parameters/cat.segments::query.format'
        - $ref: '#/components/parameters/cat.segments::query.bytes'
        - $ref: '#/components/parameters/cat.segments::query.master_timeout'
        - $ref: '#/components/parameters/cat.segments::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.segments::query.h'
        - $ref: '#/components/parameters/cat.segments::query.help'
        - $ref: '#/components/parameters/cat.segments::query.s'
        - $ref: '#/components/parameters/cat.segments::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.segments@200'
  /_cat/shards:
    get:
      operationId: cat.shards.0
      x-operation-group: cat.shards
      x-version-added: '1.0'
      description: Provides a detailed view of shard allocation on nodes.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-shards/
      parameters:
        - $ref: '#/components/parameters/cat.shards::query.format'
        - $ref: '#/components/parameters/cat.shards::query.bytes'
        - $ref: '#/components/parameters/cat.shards::query.local'
        - $ref: '#/components/parameters/cat.shards::query.master_timeout'
        - $ref: '#/components/parameters/cat.shards::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.shards::query.h'
        - $ref: '#/components/parameters/cat.shards::query.help'
        - $ref: '#/components/parameters/cat.shards::query.s'
        - $ref: '#/components/parameters/cat.shards::query.time'
        - $ref: '#/components/parameters/cat.shards::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.shards@200'
  /_cat/shards/{index}:
    get:
      operationId: cat.shards.1
      x-operation-group: cat.shards
      x-version-added: '1.0'
      description: Provides a detailed view of shard allocation on nodes.
      parameters:
        - $ref: '#/components/parameters/cat.shards::path.index'
        - $ref: '#/components/parameters/cat.shards::query.format'
        - $ref: '#/components/parameters/cat.shards::query.bytes'
        - $ref: '#/components/parameters/cat.shards::query.local'
        - $ref: '#/components/parameters/cat.shards::query.master_timeout'
        - $ref: '#/components/parameters/cat.shards::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.shards::query.h'
        - $ref: '#/components/parameters/cat.shards::query.help'
        - $ref: '#/components/parameters/cat.shards::query.s'
        - $ref: '#/components/parameters/cat.shards::query.time'
        - $ref: '#/components/parameters/cat.shards::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.shards@200'
  /_cat/snapshots:
    get:
      operationId: cat.snapshots.0
      x-operation-group: cat.snapshots
      x-version-added: '1.0'
      description: Returns all snapshots in a specific repository.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-snapshots/
      parameters:
        - $ref: '#/components/parameters/cat.snapshots::query.format'
        - $ref: '#/components/parameters/cat.snapshots::query.ignore_unavailable'
        - $ref: '#/components/parameters/cat.snapshots::query.master_timeout'
        - $ref: '#/components/parameters/cat.snapshots::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.snapshots::query.h'
        - $ref: '#/components/parameters/cat.snapshots::query.help'
        - $ref: '#/components/parameters/cat.snapshots::query.s'
        - $ref: '#/components/parameters/cat.snapshots::query.time'
        - $ref: '#/components/parameters/cat.snapshots::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.snapshots@200'
  /_cat/snapshots/{repository}:
    get:
      operationId: cat.snapshots.1
      x-operation-group: cat.snapshots
      x-version-added: '1.0'
      description: Returns all snapshots in a specific repository.
      parameters:
        - $ref: '#/components/parameters/cat.snapshots::path.repository'
        - $ref: '#/components/parameters/cat.snapshots::query.format'
        - $ref: '#/components/parameters/cat.snapshots::query.ignore_unavailable'
        - $ref: '#/components/parameters/cat.snapshots::query.master_timeout'
        - $ref: '#/components/parameters/cat.snapshots::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.snapshots::query.h'
        - $ref: '#/components/parameters/cat.snapshots::query.help'
        - $ref: '#/components/parameters/cat.snapshots::query.s'
        - $ref: '#/components/parameters/cat.snapshots::query.time'
        - $ref: '#/components/parameters/cat.snapshots::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.snapshots@200'
  /_cat/tasks:
    get:
      operationId: cat.tasks.0
      x-operation-group: cat.tasks
      x-version-added: '1.0'
      description: Returns information about the tasks currently executing on one or more nodes in the cluster.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-tasks/
      parameters:
        - $ref: '#/components/parameters/cat.tasks::query.format'
        - $ref: '#/components/parameters/cat.tasks::query.nodes'
        - $ref: '#/components/parameters/cat.tasks::query.actions'
        - $ref: '#/components/parameters/cat.tasks::query.detailed'
        - $ref: '#/components/parameters/cat.tasks::query.parent_task_id'
        - $ref: '#/components/parameters/cat.tasks::query.h'
        - $ref: '#/components/parameters/cat.tasks::query.help'
        - $ref: '#/components/parameters/cat.tasks::query.s'
        - $ref: '#/components/parameters/cat.tasks::query.time'
        - $ref: '#/components/parameters/cat.tasks::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.tasks@200'
  /_cat/templates:
    get:
      operationId: cat.templates.0
      x-operation-group: cat.templates
      x-version-added: '1.0'
      description: Returns information about existing templates.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-templates/
      parameters:
        - $ref: '#/components/parameters/cat.templates::query.format'
        - $ref: '#/components/parameters/cat.templates::query.local'
        - $ref: '#/components/parameters/cat.templates::query.master_timeout'
        - $ref: '#/components/parameters/cat.templates::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.templates::query.h'
        - $ref: '#/components/parameters/cat.templates::query.help'
        - $ref: '#/components/parameters/cat.templates::query.s'
        - $ref: '#/components/parameters/cat.templates::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.templates@200'
  /_cat/templates/{name}:
    get:
      operationId: cat.templates.1
      x-operation-group: cat.templates
      x-version-added: '1.0'
      description: Returns information about existing templates.
      parameters:
        - $ref: '#/components/parameters/cat.templates::path.name'
        - $ref: '#/components/parameters/cat.templates::query.format'
        - $ref: '#/components/parameters/cat.templates::query.local'
        - $ref: '#/components/parameters/cat.templates::query.master_timeout'
        - $ref: '#/components/parameters/cat.templates::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.templates::query.h'
        - $ref: '#/components/parameters/cat.templates::query.help'
        - $ref: '#/components/parameters/cat.templates::query.s'
        - $ref: '#/components/parameters/cat.templates::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.templates@200'
  /_cat/thread_pool:
    get:
      operationId: cat.thread_pool.0
      x-operation-group: cat.thread_pool
      x-version-added: '1.0'
      description: |-
        Returns cluster-wide thread pool statistics per node.
        By default the active, queue and rejected statistics are returned for all thread pools.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/cat/cat-thread-pool/
      parameters:
        - $ref: '#/components/parameters/cat.thread_pool::query.format'
        - $ref: '#/components/parameters/cat.thread_pool::query.size'
        - $ref: '#/components/parameters/cat.thread_pool::query.local'
        - $ref: '#/components/parameters/cat.thread_pool::query.master_timeout'
        - $ref: '#/components/parameters/cat.thread_pool::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.thread_pool::query.h'
        - $ref: '#/components/parameters/cat.thread_pool::query.help'
        - $ref: '#/components/parameters/cat.thread_pool::query.s'
        - $ref: '#/components/parameters/cat.thread_pool::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.thread_pool@200'
  /_cat/thread_pool/{thread_pool_patterns}:
    get:
      operationId: cat.thread_pool.1
      x-operation-group: cat.thread_pool
      x-version-added: '1.0'
      description: |-
        Returns cluster-wide thread pool statistics per node.
        By default the active, queue and rejected statistics are returned for all thread pools.
      parameters:
        - $ref: '#/components/parameters/cat.thread_pool::path.thread_pool_patterns'
        - $ref: '#/components/parameters/cat.thread_pool::query.format'
        - $ref: '#/components/parameters/cat.thread_pool::query.size'
        - $ref: '#/components/parameters/cat.thread_pool::query.local'
        - $ref: '#/components/parameters/cat.thread_pool::query.master_timeout'
        - $ref: '#/components/parameters/cat.thread_pool::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/cat.thread_pool::query.h'
        - $ref: '#/components/parameters/cat.thread_pool::query.help'
        - $ref: '#/components/parameters/cat.thread_pool::query.s'
        - $ref: '#/components/parameters/cat.thread_pool::query.v'
      responses:
        '200':
          $ref: '#/components/responses/cat.thread_pool@200'
components:
  requestBodies:
    cat.pit_segments:
      content:
        application/json:
          schema:
            type: object
            properties:
              pit_id:
                type: array
                items:
                  type: string
            required:
              - pit_id
  responses:
    cat.aliases@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.aliases.yaml#/components/schemas/AliasesRecord'
    cat.all_pit_segments@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat._common.yaml#/components/schemas/CatPitSegmentsRecord'
    cat.allocation@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.allocation.yaml#/components/schemas/AllocationRecord'
    cat.cluster_manager@200:
      description: ''
    cat.count@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.count.yaml#/components/schemas/CountRecord'
    cat.fielddata@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.fielddata.yaml#/components/schemas/FielddataRecord'
    cat.health@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.health.yaml#/components/schemas/HealthRecord'
    cat.help@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.help.yaml#/components/schemas/HelpRecord'
    cat.indices@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.indices.yaml#/components/schemas/IndicesRecord'
    cat.master@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.master.yaml#/components/schemas/MasterRecord'
    cat.nodeattrs@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.nodeattrs.yaml#/components/schemas/NodeAttributesRecord'
    cat.nodes@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.nodes.yaml#/components/schemas/NodesRecord'
    cat.pending_tasks@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.pending_tasks.yaml#/components/schemas/PendingTasksRecord'
    cat.pit_segments@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat._common.yaml#/components/schemas/CatPitSegmentsRecord'
    cat.plugins@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.plugins.yaml#/components/schemas/PluginsRecord'
    cat.recovery@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.recovery.yaml#/components/schemas/RecoveryRecord'
    cat.repositories@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.repositories.yaml#/components/schemas/RepositoriesRecord'
    cat.segment_replication@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat._common.yaml#/components/schemas/CatSegmentReplicationRecord'
    cat.segments@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.segments.yaml#/components/schemas/SegmentsRecord'
    cat.shards@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.shards.yaml#/components/schemas/ShardsRecord'
    cat.snapshots@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.snapshots.yaml#/components/schemas/SnapshotsRecord'
    cat.tasks@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.tasks.yaml#/components/schemas/TasksRecord'
    cat.templates@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.templates.yaml#/components/schemas/TemplatesRecord'
    cat.thread_pool@200:
      description: ''
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/cat.thread_pool.yaml#/components/schemas/ThreadPoolRecord'
  parameters:
    cat.aliases::path.name:
      in: path
      name: name
      description: A comma-separated list of aliases to retrieve. Supports wildcards (`*`).  To retrieve all aliases, omit
        this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    cat.aliases::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: Whether to expand wildcard expression to concrete indices that are open, closed or both.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    cat.aliases::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.aliases::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.aliases::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.aliases::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.aliases::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.aliases::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.all_pit_segments::query.bytes:
      name: bytes
      in: query
      description: The unit in which to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
    cat.all_pit_segments::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.all_pit_segments::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.all_pit_segments::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.all_pit_segments::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.all_pit_segments::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.allocation::path.node_id:
      in: path
      name: node_id
      description: Comma-separated list of node identifiers or names used to limit the returned information.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/NodeIds'
      style: simple
    cat.allocation::query.bytes:
      in: query
      name: bytes
      description: The unit used to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
      style: form
    cat.allocation::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.allocation::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.allocation::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.allocation::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.allocation::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.allocation::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.allocation::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.allocation::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.cluster_manager::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.cluster_manager::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.cluster_manager::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.cluster_manager::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.cluster_manager::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.cluster_manager::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.cluster_manager::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.cluster_manager::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.count::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`). To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    cat.count::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.count::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.count::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.count::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.count::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.fielddata::path.fields:
      in: path
      name: fields
      description: |-
        Comma-separated list of fields used to limit returned information.
        To retrieve all fields, omit this parameter.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Fields'
      style: simple
    cat.fielddata::query.bytes:
      in: query
      name: bytes
      description: The unit used to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
      style: form
    cat.fielddata::query.fields:
      in: query
      name: fields
      description: Comma-separated list of fields used to limit returned information.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Fields'
      style: form
    cat.fielddata::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.fielddata::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.fielddata::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.fielddata::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.fielddata::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.health::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.health::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.health::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.health::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.health::query.time:
      in: query
      name: time
      description: The unit used to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
      style: form
    cat.health::query.ts:
      in: query
      name: ts
      description: If true, returns `HH:MM:SS` and Unix epoch timestamps.
      schema:
        type: boolean
      style: form
    cat.health::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.help::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.help::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.indices::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`). To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    cat.indices::query.bytes:
      in: query
      name: bytes
      description: The unit used to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
      style: form
    cat.indices::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.indices::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: The type of index that wildcard patterns can match.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    cat.indices::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.indices::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.indices::query.health:
      in: query
      name: health
      description: The health status used to limit returned indices. By default, the response includes indices of any health status.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/HealthStatus'
      style: form
    cat.indices::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.indices::query.include_unloaded_segments:
      in: query
      name: include_unloaded_segments
      description: If true, the response includes information from segments that are not loaded into memory.
      schema:
        type: boolean
      style: form
    cat.indices::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.indices::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.indices::query.pri:
      in: query
      name: pri
      description: If true, the response only includes information from primary shards.
      schema:
        type: boolean
      style: form
    cat.indices::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.indices::query.time:
      in: query
      name: time
      description: The unit used to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
      style: form
    cat.indices::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.master::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.master::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.master::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.master::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.master::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.master::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.master::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.master::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.nodeattrs::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.nodeattrs::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.nodeattrs::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.nodeattrs::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.nodeattrs::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.nodeattrs::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.nodeattrs::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.nodeattrs::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.nodes::query.bytes:
      in: query
      name: bytes
      description: The unit used to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
      style: form
    cat.nodes::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.nodes::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.nodes::query.full_id:
      in: query
      name: full_id
      description: If `true`, return the full node ID. If `false`, return the shortened node ID.
      schema:
        oneOf:
          - type: boolean
          - type: string
      style: form
    cat.nodes::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.nodes::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.nodes::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
        x-version-deprecated: '1.0'
        x-deprecation-message: This parameter does not cause this API to act locally.
        deprecated: true
    cat.nodes::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.nodes::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.nodes::query.time:
      name: time
      in: query
      description: The unit in which to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
    cat.nodes::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.pending_tasks::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.pending_tasks::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.pending_tasks::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.pending_tasks::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.pending_tasks::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.pending_tasks::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.pending_tasks::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.pending_tasks::query.time:
      name: time
      in: query
      description: The unit in which to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
    cat.pending_tasks::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.pit_segments::query.bytes:
      name: bytes
      in: query
      description: The unit in which to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
    cat.pit_segments::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.pit_segments::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.pit_segments::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.pit_segments::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.pit_segments::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.plugins::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.plugins::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.plugins::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.plugins::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.plugins::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.plugins::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.plugins::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.plugins::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.recovery::path.index:
      in: path
      name: index
      description: |-
        A comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`). To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    cat.recovery::query.active_only:
      in: query
      name: active_only
      description: If `true`, the response only includes ongoing shard recoveries.
      schema:
        type: boolean
      style: form
    cat.recovery::query.bytes:
      in: query
      name: bytes
      description: The unit used to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
      style: form
    cat.recovery::query.detailed:
      in: query
      name: detailed
      description: If `true`, the response includes detailed information about shard recoveries.
      schema:
        type: boolean
      style: form
    cat.recovery::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.recovery::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.recovery::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.recovery::query.index:
      name: index
      in: query
      description: Comma-separated list or wildcard expression of index names to limit the returned information.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list or wildcard expression of index names to limit the returned information.
      explode: true
    cat.recovery::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.recovery::query.time:
      name: time
      in: query
      description: The unit in which to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
    cat.recovery::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.repositories::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.repositories::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.repositories::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.repositories::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.repositories::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.repositories::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.repositories::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.repositories::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.segment_replication::path.index:
      name: index
      in: path
      description: Comma-separated list or wildcard expression of index names to limit the returned information.
      schema:
        type: string
        pattern: ^(?!_|template|query|field|point|clear|usage|stats|hot|reload|painless).+$
        description: Comma-separated list or wildcard expression of index names to limit the returned information.
        x-data-type: array
      required: true
    cat.segment_replication::query.active_only:
      name: active_only
      in: query
      description: If `true`, the response only includes ongoing segment replication events.
      schema:
        type: boolean
        default: false
        description: If `true`, the response only includes ongoing segment replication events.
    cat.segment_replication::query.allow_no_indices:
      name: allow_no_indices
      in: query
      description: Whether to ignore if a wildcard indices expression resolves into no concrete indices. (This includes `_all`
        string or when no indices have been specified).
      schema:
        type: boolean
        description: Whether to ignore if a wildcard indices expression resolves into no concrete indices. (This includes `_all`
          string or when no indices have been specified).
    cat.segment_replication::query.bytes:
      name: bytes
      in: query
      description: The unit in which to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
    cat.segment_replication::query.completed_only:
      name: completed_only
      in: query
      description: If `true`, the response only includes latest completed segment replication events.
      schema:
        type: boolean
        default: false
        description: If `true`, the response only includes latest completed segment replication events.
    cat.segment_replication::query.detailed:
      name: detailed
      in: query
      description: If `true`, the response includes detailed information about segment replications.
      schema:
        type: boolean
        default: false
        description: If `true`, the response includes detailed information about segment replications.
    cat.segment_replication::query.expand_wildcards:
      name: expand_wildcards
      in: query
      description: Whether to expand wildcard expression to concrete indices that are open, closed or both.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
    cat.segment_replication::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.segment_replication::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.segment_replication::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.segment_replication::query.ignore_throttled:
      name: ignore_throttled
      in: query
      description: Whether specified concrete, expanded or aliased indices should be ignored when throttled.
      schema:
        type: boolean
        description: Whether specified concrete, expanded or aliased indices should be ignored when throttled.
    cat.segment_replication::query.ignore_unavailable:
      name: ignore_unavailable
      in: query
      description: Whether specified concrete indices should be ignored when unavailable (missing or closed).
      schema:
        type: boolean
        description: Whether specified concrete indices should be ignored when unavailable (missing or closed).
    cat.segment_replication::query.index:
      name: index
      in: query
      description: Comma-separated list or wildcard expression of index names to limit the returned information.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list or wildcard expression of index names to limit the returned information.
      explode: true
    cat.segment_replication::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.segment_replication::query.shards:
      name: shards
      in: query
      description: Comma-separated list of shards to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of shards to display.
      explode: true
    cat.segment_replication::query.time:
      name: time
      in: query
      description: The unit in which to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
    cat.segment_replication::query.timeout:
      name: timeout
      in: query
      description: Operation timeout.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
    cat.segment_replication::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.segments::path.index:
      in: path
      name: index
      description: |-
        A comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    cat.segments::query.bytes:
      in: query
      name: bytes
      description: The unit used to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
      style: form
    cat.segments::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.segments::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.segments::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.segments::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.segments::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.segments::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.segments::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.shards::path.index:
      in: path
      name: index
      description: |-
        A comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    cat.shards::query.bytes:
      in: query
      name: bytes
      description: The unit used to display byte values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Bytes'
      style: form
    cat.shards::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.shards::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.shards::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.shards::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.shards::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.shards::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.shards::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.shards::query.time:
      name: time
      in: query
      description: The unit in which to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
    cat.shards::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.snapshots::path.repository:
      in: path
      name: repository
      description: |-
        A comma-separated list of snapshot repositories used to limit the request.
        Accepts wildcard expressions.
        `_all` returns all repositories.
        If any repository fails during the request, Opensearch returns an error.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    cat.snapshots::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.snapshots::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.snapshots::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.snapshots::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.snapshots::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `true`, the response does not include information from unavailable snapshots.
      schema:
        type: boolean
      style: form
    cat.snapshots::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.snapshots::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.snapshots::query.time:
      name: time
      in: query
      description: The unit in which to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
    cat.snapshots::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.tasks::query.actions:
      in: query
      name: actions
      description: The task action names, which are used to limit the response.
      schema:
        type: array
        items:
          type: string
      style: form
    cat.tasks::query.detailed:
      in: query
      name: detailed
      description: If `true`, the response includes detailed information about shard recoveries.
      schema:
        type: boolean
      style: form
    cat.tasks::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.tasks::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.tasks::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.tasks::query.nodes:
      name: nodes
      in: query
      description: Comma-separated list of node IDs or names to limit the returned information; use `_local` to return
        information from the node you're connecting to, leave empty to get information from all nodes.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of node IDs or names to limit the returned information; use `_local` to return
          information from the node you're connecting to, leave empty to get information from all nodes.
      explode: true
    cat.tasks::query.parent_task_id:
      in: query
      name: parent_task_id
      description: The parent task identifier, which is used to limit the response.
      schema:
        type: string
      style: form
    cat.tasks::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.tasks::query.time:
      name: time
      in: query
      description: The unit in which to display time values.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/TimeUnit'
    cat.tasks::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.templates::path.name:
      in: path
      name: name
      description: |-
        The name of the template to return.
        Accepts wildcard expressions. If omitted, all templates are returned.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    cat.templates::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.templates::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.templates::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.templates::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.templates::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.templates::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.templates::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.templates::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
    cat.thread_pool::path.thread_pool_patterns:
      in: path
      name: thread_pool_patterns
      description: |-
        A comma-separated list of thread pool names used to limit the request.
        Accepts wildcard expressions.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    cat.thread_pool::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    cat.thread_pool::query.format:
      name: format
      in: query
      description: A short version of the Accept header, e.g. json, yaml.
      schema:
        type: string
        description: A short version of the Accept header, e.g. json, yaml.
    cat.thread_pool::query.h:
      name: h
      in: query
      description: Comma-separated list of column names to display.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names to display.
      explode: true
    cat.thread_pool::query.help:
      name: help
      in: query
      description: Return help information.
      schema:
        type: boolean
        default: false
        description: Return help information.
    cat.thread_pool::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    cat.thread_pool::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    cat.thread_pool::query.s:
      name: s
      in: query
      description: Comma-separated list of column names or column aliases to sort by.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of column names or column aliases to sort by.
      explode: true
    cat.thread_pool::query.size:
      name: size
      in: query
      description: The multiplier in which to display values.
      schema:
        type: integer
        description: The multiplier in which to display values.
        format: int32
    cat.thread_pool::query.v:
      name: v
      in: query
      description: Verbose mode. Display column headers.
      schema:
        type: boolean
        default: false
        description: Verbose mode. Display column headers.
