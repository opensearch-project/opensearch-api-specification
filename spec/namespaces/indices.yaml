openapi: 3.1.0
info:
  title: OpenSearch Indices API
  description: OpenSearch Indices API
  version: 1.0.0
paths:
  /_alias:
    get:
      operationId: indices.get_alias.0
      x-operation-group: indices.get_alias
      x-version-added: '1.0'
      description: Returns an alias.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-alias/
      parameters:
        - $ref: '#/components/parameters/indices.get_alias::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_alias::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_alias::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_alias::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_alias@200'
  /_alias/{name}:
    get:
      operationId: indices.get_alias.1
      x-operation-group: indices.get_alias
      x-version-added: '1.0'
      description: Returns an alias.
      parameters:
        - $ref: '#/components/parameters/indices.get_alias::path.name'
        - $ref: '#/components/parameters/indices.get_alias::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_alias::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_alias::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_alias::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_alias@200'
    head:
      operationId: indices.exists_alias.0
      x-operation-group: indices.exists_alias
      x-version-added: '1.0'
      description: Returns information about whether a particular alias exists.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.exists_alias::path.name'
        - $ref: '#/components/parameters/indices.exists_alias::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.exists_alias::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.exists_alias::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.exists_alias::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.exists_alias@200'
  /_aliases:
    post:
      operationId: indices.update_aliases.0
      x-operation-group: indices.update_aliases
      x-version-added: '1.0'
      description: Updates index aliases.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/alias/
      parameters:
        - $ref: '#/components/parameters/indices.update_aliases::query.timeout'
        - $ref: '#/components/parameters/indices.update_aliases::query.master_timeout'
        - $ref: '#/components/parameters/indices.update_aliases::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.update_aliases'
      responses:
        '200':
          $ref: '#/components/responses/indices.update_aliases@200'
  /_analyze:
    get:
      operationId: indices.analyze.0
      x-operation-group: indices.analyze
      x-version-added: '1.0'
      description: Performs the analysis process on a text and return the tokens breakdown of the text.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/analyze-apis/perform-text-analysis/
      parameters:
        - $ref: '#/components/parameters/indices.analyze::query.index'
      requestBody:
        $ref: '#/components/requestBodies/indices.analyze'
      responses:
        '200':
          $ref: '#/components/responses/indices.analyze@200'
    post:
      operationId: indices.analyze.1
      x-operation-group: indices.analyze
      x-version-added: '1.0'
      description: Performs the analysis process on a text and return the tokens breakdown of the text.
      parameters:
        - $ref: '#/components/parameters/indices.analyze::query.index'
      requestBody:
        $ref: '#/components/requestBodies/indices.analyze'
      responses:
        '200':
          $ref: '#/components/responses/indices.analyze@200'
  /_cache/clear:
    post:
      operationId: indices.clear_cache.0
      x-operation-group: indices.clear_cache
      x-version-added: '1.0'
      description: Clears all or specific caches for one or more indices.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/clear-index-cache/
      parameters:
        - $ref: '#/components/parameters/indices.clear_cache::query.fielddata'
        - $ref: '#/components/parameters/indices.clear_cache::query.fields'
        - $ref: '#/components/parameters/indices.clear_cache::query.query'
        - $ref: '#/components/parameters/indices.clear_cache::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.clear_cache::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.clear_cache::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.clear_cache::query.index'
        - $ref: '#/components/parameters/indices.clear_cache::query.request'
      responses:
        '200':
          $ref: '#/components/responses/indices.clear_cache@200'
  /_data_stream:
    get:
      operationId: indices.get_data_stream.0
      x-operation-group: indices.get_data_stream
      x-version-added: '1.0'
      description: Returns data streams.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/data-streams/
      parameters: []
      responses:
        '200':
          $ref: '#/components/responses/indices.get_data_stream@200'
  /_data_stream/_stats:
    get:
      operationId: indices.data_streams_stats.0
      x-operation-group: indices.data_streams_stats
      x-version-added: '1.0'
      description: Provides statistics on operations happening in a data stream.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/data-streams/
      parameters: []
      responses:
        '200':
          $ref: '#/components/responses/indices.data_streams_stats@200'
  /_data_stream/{name}:
    delete:
      operationId: indices.delete_data_stream.0
      x-operation-group: indices.delete_data_stream
      x-version-added: '1.0'
      description: Deletes a data stream.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/data-streams/
      parameters:
        - $ref: '#/components/parameters/indices.delete_data_stream::path.name'
      responses:
        '200':
          $ref: '#/components/responses/indices.delete_data_stream@200'
    get:
      operationId: indices.get_data_stream.1
      x-operation-group: indices.get_data_stream
      x-version-added: '1.0'
      description: Returns data streams.
      parameters:
        - $ref: '#/components/parameters/indices.get_data_stream::path.name'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_data_stream@200'
    put:
      operationId: indices.create_data_stream.0
      x-operation-group: indices.create_data_stream
      x-version-added: '1.0'
      description: Creates or updates a data stream.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/data-streams/
      parameters:
        - $ref: '#/components/parameters/indices.create_data_stream::path.name'
      requestBody:
        $ref: '#/components/requestBodies/indices.create_data_stream'
      responses:
        '200':
          $ref: '#/components/responses/indices.create_data_stream@200'
  /_data_stream/{name}/_stats:
    get:
      operationId: indices.data_streams_stats.1
      x-operation-group: indices.data_streams_stats
      x-version-added: '1.0'
      description: Provides statistics on operations happening in a data stream.
      parameters:
        - $ref: '#/components/parameters/indices.data_streams_stats::path.name'
      responses:
        '200':
          $ref: '#/components/responses/indices.data_streams_stats@200'
  /_flush:
    get:
      operationId: indices.flush.0
      x-operation-group: indices.flush
      x-version-added: '1.0'
      description: Performs the flush operation on one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.flush::query.force'
        - $ref: '#/components/parameters/indices.flush::query.wait_if_ongoing'
        - $ref: '#/components/parameters/indices.flush::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.flush::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.flush::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.flush@200'
    post:
      operationId: indices.flush.1
      x-operation-group: indices.flush
      x-version-added: '1.0'
      description: Performs the flush operation on one or more indices.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.flush::query.force'
        - $ref: '#/components/parameters/indices.flush::query.wait_if_ongoing'
        - $ref: '#/components/parameters/indices.flush::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.flush::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.flush::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.flush@200'
  /_forcemerge:
    post:
      operationId: indices.forcemerge.0
      x-operation-group: indices.forcemerge
      x-version-added: '1.0'
      description: Performs the force merge operation on one or more indices.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.forcemerge::query.flush'
        - $ref: '#/components/parameters/indices.forcemerge::query.primary_only'
        - $ref: '#/components/parameters/indices.forcemerge::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.forcemerge::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.forcemerge::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.forcemerge::query.max_num_segments'
        - $ref: '#/components/parameters/indices.forcemerge::query.only_expunge_deletes'
        - $ref: '#/components/parameters/indices.forcemerge::query.wait_for_completion'
      responses:
        '200':
          $ref: '#/components/responses/indices.forcemerge@200'
  /_index_template:
    get:
      operationId: indices.get_index_template.0
      x-operation-group: indices.get_index_template
      x-version-added: '1.0'
      description: Returns an index template.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-templates/
      parameters:
        - $ref: '#/components/parameters/indices.get_index_template::query.flat_settings'
        - $ref: '#/components/parameters/indices.get_index_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_index_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_index_template::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_index_template@200'
  /_index_template/_simulate:
    post:
      operationId: indices.simulate_template.0
      x-operation-group: indices.simulate_template
      x-version-added: '1.0'
      description: Simulate resolving the given template name or body.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.simulate_template::query.create'
        - $ref: '#/components/parameters/indices.simulate_template::query.cause'
        - $ref: '#/components/parameters/indices.simulate_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.simulate_template::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.simulate_template'
      responses:
        '200':
          $ref: '#/components/responses/indices.simulate_template@200'
  /_index_template/_simulate/{name}:
    post:
      operationId: indices.simulate_template.1
      x-operation-group: indices.simulate_template
      x-version-added: '1.0'
      description: Simulate resolving the given template name or body.
      parameters:
        - $ref: '#/components/parameters/indices.simulate_template::path.name'
        - $ref: '#/components/parameters/indices.simulate_template::query.create'
        - $ref: '#/components/parameters/indices.simulate_template::query.cause'
        - $ref: '#/components/parameters/indices.simulate_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.simulate_template::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.simulate_template'
      responses:
        '200':
          $ref: '#/components/responses/indices.simulate_template@200'
  /_index_template/_simulate_index/{name}:
    post:
      operationId: indices.simulate_index_template.0
      x-operation-group: indices.simulate_index_template
      x-version-added: '1.0'
      description: Simulate matching the given index name against the index templates in the system.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.simulate_index_template::path.name'
        - $ref: '#/components/parameters/indices.simulate_index_template::query.create'
        - $ref: '#/components/parameters/indices.simulate_index_template::query.cause'
        - $ref: '#/components/parameters/indices.simulate_index_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.simulate_index_template::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.simulate_index_template'
      responses:
        '200':
          $ref: '#/components/responses/indices.simulate_index_template@200'
  /_index_template/{name}:
    delete:
      operationId: indices.delete_index_template.0
      x-operation-group: indices.delete_index_template
      x-version-added: '1.0'
      description: Deletes an index template.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-templates/#delete-a-template
      parameters:
        - $ref: '#/components/parameters/indices.delete_index_template::path.name'
        - $ref: '#/components/parameters/indices.delete_index_template::query.timeout'
        - $ref: '#/components/parameters/indices.delete_index_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.delete_index_template::query.cluster_manager_timeout'
      responses:
        '200':
          $ref: '#/components/responses/indices.delete_index_template@200'
    get:
      operationId: indices.get_index_template.1
      x-operation-group: indices.get_index_template
      x-version-added: '1.0'
      description: Returns an index template.
      parameters:
        - $ref: '#/components/parameters/indices.get_index_template::path.name'
        - $ref: '#/components/parameters/indices.get_index_template::query.flat_settings'
        - $ref: '#/components/parameters/indices.get_index_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_index_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_index_template::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_index_template@200'
    head:
      operationId: indices.exists_index_template.0
      x-operation-group: indices.exists_index_template
      x-version-added: '1.0'
      description: Returns information about whether a particular index template exists.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-templates/
      parameters:
        - $ref: '#/components/parameters/indices.exists_index_template::path.name'
        - $ref: '#/components/parameters/indices.exists_index_template::query.flat_settings'
        - $ref: '#/components/parameters/indices.exists_index_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.exists_index_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.exists_index_template::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.exists_index_template@200'
    post:
      operationId: indices.put_index_template.0
      x-operation-group: indices.put_index_template
      x-version-added: '1.0'
      description: Creates or updates an index template.
      parameters:
        - $ref: '#/components/parameters/indices.put_index_template::path.name'
        - $ref: '#/components/parameters/indices.put_index_template::query.create'
        - $ref: '#/components/parameters/indices.put_index_template::query.cause'
        - $ref: '#/components/parameters/indices.put_index_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_index_template::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_index_template'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_index_template@200'
    put:
      operationId: indices.put_index_template.1
      x-operation-group: indices.put_index_template
      x-version-added: '1.0'
      description: Creates or updates an index template.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-templates/
      parameters:
        - $ref: '#/components/parameters/indices.put_index_template::path.name'
        - $ref: '#/components/parameters/indices.put_index_template::query.create'
        - $ref: '#/components/parameters/indices.put_index_template::query.cause'
        - $ref: '#/components/parameters/indices.put_index_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_index_template::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_index_template'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_index_template@200'
  /_mapping:
    get:
      operationId: indices.get_mapping.0
      x-operation-group: indices.get_mapping
      x-version-added: '1.0'
      description: Returns mappings for one or more indices.
      externalDocs:
        url: https://opensearch.org/docs/latest/field-types/index/#get-a-mapping
      parameters:
        - $ref: '#/components/parameters/indices.get_mapping::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_mapping::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_mapping::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_mapping::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_mapping::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_mapping::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_mapping@200'
  /_mapping/field/{fields}:
    get:
      operationId: indices.get_field_mapping.0
      x-operation-group: indices.get_field_mapping
      x-version-added: '1.0'
      description: Returns mapping for one or more fields.
      externalDocs:
        url: https://opensearch.org/docs/latest/field-types/index/
      parameters:
        - $ref: '#/components/parameters/indices.get_field_mapping::path.fields'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.include_defaults'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_field_mapping@200'
  /_recovery:
    get:
      operationId: indices.recovery.0
      x-operation-group: indices.recovery
      x-version-added: '1.0'
      description: Returns information about ongoing index shard recoveries.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.recovery::query.detailed'
        - $ref: '#/components/parameters/indices.recovery::query.active_only'
      responses:
        '200':
          $ref: '#/components/responses/indices.recovery@200'
  /_refresh:
    get:
      operationId: indices.refresh.0
      x-operation-group: indices.refresh
      x-version-added: '1.0'
      description: Performs the refresh operation in one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.refresh::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.refresh::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.refresh::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.refresh@200'
    post:
      operationId: indices.refresh.1
      x-operation-group: indices.refresh
      x-version-added: '1.0'
      description: Performs the refresh operation in one or more indices.
      externalDocs:
        url: https://opensearch.org/docs/latest/tuning-your-cluster/availability-and-recovery/remote-store/index/#refresh-level-and-request-level-durability
      parameters:
        - $ref: '#/components/parameters/indices.refresh::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.refresh::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.refresh::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.refresh@200'
  /_resolve/index/{name}:
    get:
      operationId: indices.resolve_index.0
      x-operation-group: indices.resolve_index
      x-version-added: '1.0'
      description: Returns information about any matching indices, aliases, and data streams.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.resolve_index::path.name'
        - $ref: '#/components/parameters/indices.resolve_index::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.resolve_index@200'
  /_segments:
    get:
      operationId: indices.segments.0
      x-operation-group: indices.segments
      x-version-added: '1.0'
      description: Provides low-level information about segments in a Lucene index.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.segments::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.segments::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.segments::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.segments::query.verbose'
      responses:
        '200':
          $ref: '#/components/responses/indices.segments@200'
  /_settings:
    get:
      operationId: indices.get_settings.0
      x-operation-group: indices.get_settings
      x-version-added: '1.0'
      description: Returns settings for one or more indices.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/get-settings/
      parameters:
        - $ref: '#/components/parameters/indices.get_settings::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_settings::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_settings::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_settings::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_settings::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_settings::query.flat_settings'
        - $ref: '#/components/parameters/indices.get_settings::query.local'
        - $ref: '#/components/parameters/indices.get_settings::query.include_defaults'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_settings@200'
    put:
      operationId: indices.put_settings.0
      x-operation-group: indices.put_settings
      x-version-added: '1.0'
      description: Updates the index settings.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/update-settings/
      parameters:
        - $ref: '#/components/parameters/indices.put_settings::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_settings::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.put_settings::query.timeout'
        - $ref: '#/components/parameters/indices.put_settings::query.preserve_existing'
        - $ref: '#/components/parameters/indices.put_settings::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.put_settings::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.put_settings::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.put_settings::query.flat_settings'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_settings'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_settings@200'
  /_settings/{name}:
    get:
      operationId: indices.get_settings.1
      x-operation-group: indices.get_settings
      x-version-added: '1.0'
      description: Returns settings for one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.get_settings::path.name'
        - $ref: '#/components/parameters/indices.get_settings::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_settings::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_settings::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_settings::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_settings::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_settings::query.flat_settings'
        - $ref: '#/components/parameters/indices.get_settings::query.local'
        - $ref: '#/components/parameters/indices.get_settings::query.include_defaults'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_settings@200'
  /_shard_stores:
    get:
      operationId: indices.shard_stores.0
      x-operation-group: indices.shard_stores
      x-version-added: '1.0'
      description: Provides store information for shard copies of indices.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.shard_stores::query.status'
        - $ref: '#/components/parameters/indices.shard_stores::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.shard_stores::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.shard_stores::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.shard_stores@200'
  /_stats:
    get:
      operationId: indices.stats.0
      x-operation-group: indices.stats
      x-version-added: '1.0'
      description: Provides statistics on operations happening in an index.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.stats::query.completion_fields'
        - $ref: '#/components/parameters/indices.stats::query.fielddata_fields'
        - $ref: '#/components/parameters/indices.stats::query.fields'
        - $ref: '#/components/parameters/indices.stats::query.groups'
        - $ref: '#/components/parameters/indices.stats::query.level'
        - $ref: '#/components/parameters/indices.stats::query.include_segment_file_sizes'
        - $ref: '#/components/parameters/indices.stats::query.include_unloaded_segments'
        - $ref: '#/components/parameters/indices.stats::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.stats::query.forbid_closed_indices'
      responses:
        '200':
          $ref: '#/components/responses/indices.stats@200'
  /_stats/{metric}:
    get:
      operationId: indices.stats.1
      x-operation-group: indices.stats
      x-version-added: '1.0'
      description: Provides statistics on operations happening in an index.
      parameters:
        - $ref: '#/components/parameters/indices.stats::path.metric'
        - $ref: '#/components/parameters/indices.stats::query.completion_fields'
        - $ref: '#/components/parameters/indices.stats::query.fielddata_fields'
        - $ref: '#/components/parameters/indices.stats::query.fields'
        - $ref: '#/components/parameters/indices.stats::query.groups'
        - $ref: '#/components/parameters/indices.stats::query.level'
        - $ref: '#/components/parameters/indices.stats::query.include_segment_file_sizes'
        - $ref: '#/components/parameters/indices.stats::query.include_unloaded_segments'
        - $ref: '#/components/parameters/indices.stats::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.stats::query.forbid_closed_indices'
      responses:
        '200':
          $ref: '#/components/responses/indices.stats@200'
  /_template:
    get:
      operationId: indices.get_template.0
      x-operation-group: indices.get_template
      x-version-added: '1.0'
      description: Returns an index template.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.get_template::query.flat_settings'
        - $ref: '#/components/parameters/indices.get_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_template::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_template@200'
  /_template/{name}:
    delete:
      operationId: indices.delete_template.0
      x-operation-group: indices.delete_template
      x-version-added: '1.0'
      description: Deletes an index template.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.delete_template::path.name'
        - $ref: '#/components/parameters/indices.delete_template::query.timeout'
        - $ref: '#/components/parameters/indices.delete_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.delete_template::query.cluster_manager_timeout'
      responses:
        '200':
          $ref: '#/components/responses/indices.delete_template@200'
    get:
      operationId: indices.get_template.1
      x-operation-group: indices.get_template
      x-version-added: '1.0'
      description: Returns an index template.
      parameters:
        - $ref: '#/components/parameters/indices.get_template::path.name'
        - $ref: '#/components/parameters/indices.get_template::query.flat_settings'
        - $ref: '#/components/parameters/indices.get_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_template::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_template@200'
    head:
      operationId: indices.exists_template.0
      x-operation-group: indices.exists_template
      x-version-added: '1.0'
      description: Returns information about whether a particular index template exists.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.exists_template::path.name'
        - $ref: '#/components/parameters/indices.exists_template::query.flat_settings'
        - $ref: '#/components/parameters/indices.exists_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.exists_template::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.exists_template::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.exists_template@200'
    post:
      operationId: indices.put_template.0
      x-operation-group: indices.put_template
      x-version-added: '1.0'
      description: Creates or updates an index template.
      parameters:
        - $ref: '#/components/parameters/indices.put_template::path.name'
        - $ref: '#/components/parameters/indices.put_template::query.order'
        - $ref: '#/components/parameters/indices.put_template::query.create'
        - $ref: '#/components/parameters/indices.put_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_template::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_template'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_template@200'
    put:
      operationId: indices.put_template.1
      x-operation-group: indices.put_template
      x-version-added: '1.0'
      description: Creates or updates an index template.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-templates/
      parameters:
        - $ref: '#/components/parameters/indices.put_template::path.name'
        - $ref: '#/components/parameters/indices.put_template::query.order'
        - $ref: '#/components/parameters/indices.put_template::query.create'
        - $ref: '#/components/parameters/indices.put_template::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_template::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_template'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_template@200'
  /_upgrade:
    get:
      operationId: indices.get_upgrade.0
      x-operation-group: indices.get_upgrade
      x-version-added: '1.0'
      description: The _upgrade API is no longer useful and will be removed.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.get_upgrade::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_upgrade::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_upgrade::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_upgrade@200'
    post:
      operationId: indices.upgrade.0
      x-operation-group: indices.upgrade
      x-version-added: '1.0'
      description: The _upgrade API is no longer useful and will be removed.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.upgrade::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.upgrade::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.upgrade::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.upgrade::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.upgrade::query.only_ancient_segments'
      responses:
        '200':
          $ref: '#/components/responses/indices.upgrade@200'
  /_validate/query:
    get:
      operationId: indices.validate_query.0
      x-operation-group: indices.validate_query
      x-version-added: '1.0'
      description: Allows a user to validate a potentially expensive query without executing it.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.validate_query::query.explain'
        - $ref: '#/components/parameters/indices.validate_query::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.validate_query::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.validate_query::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.validate_query::query.q'
        - $ref: '#/components/parameters/indices.validate_query::query.analyzer'
        - $ref: '#/components/parameters/indices.validate_query::query.analyze_wildcard'
        - $ref: '#/components/parameters/indices.validate_query::query.default_operator'
        - $ref: '#/components/parameters/indices.validate_query::query.df'
        - $ref: '#/components/parameters/indices.validate_query::query.lenient'
        - $ref: '#/components/parameters/indices.validate_query::query.rewrite'
        - $ref: '#/components/parameters/indices.validate_query::query.all_shards'
      requestBody:
        $ref: '#/components/requestBodies/indices.validate_query'
      responses:
        '200':
          $ref: '#/components/responses/indices.validate_query@200'
    post:
      operationId: indices.validate_query.1
      x-operation-group: indices.validate_query
      x-version-added: '1.0'
      description: Allows a user to validate a potentially expensive query without executing it.
      parameters:
        - $ref: '#/components/parameters/indices.validate_query::query.explain'
        - $ref: '#/components/parameters/indices.validate_query::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.validate_query::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.validate_query::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.validate_query::query.q'
        - $ref: '#/components/parameters/indices.validate_query::query.analyzer'
        - $ref: '#/components/parameters/indices.validate_query::query.analyze_wildcard'
        - $ref: '#/components/parameters/indices.validate_query::query.default_operator'
        - $ref: '#/components/parameters/indices.validate_query::query.df'
        - $ref: '#/components/parameters/indices.validate_query::query.lenient'
        - $ref: '#/components/parameters/indices.validate_query::query.rewrite'
        - $ref: '#/components/parameters/indices.validate_query::query.all_shards'
      requestBody:
        $ref: '#/components/requestBodies/indices.validate_query'
      responses:
        '200':
          $ref: '#/components/responses/indices.validate_query@200'
  /{alias}/_rollover:
    post:
      operationId: indices.rollover.0
      x-operation-group: indices.rollover
      x-version-added: '1.0'
      description: |-
        Updates an alias to point to a new index when the existing index
        is considered to be too large or too old.
      externalDocs:
        url: https://opensearch.org/docs/latest/dashboards/im-dashboards/rollover/
      parameters:
        - $ref: '#/components/parameters/indices.rollover::path.alias'
        - $ref: '#/components/parameters/indices.rollover::query.timeout'
        - $ref: '#/components/parameters/indices.rollover::query.dry_run'
        - $ref: '#/components/parameters/indices.rollover::query.master_timeout'
        - $ref: '#/components/parameters/indices.rollover::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.rollover::query.wait_for_active_shards'
      requestBody:
        $ref: '#/components/requestBodies/indices.rollover'
      responses:
        '200':
          $ref: '#/components/responses/indices.rollover@200'
  /{alias}/_rollover/{new_index}:
    post:
      operationId: indices.rollover.1
      x-operation-group: indices.rollover
      x-version-added: '1.0'
      description: |-
        Updates an alias to point to a new index when the existing index
        is considered to be too large or too old.
      parameters:
        - $ref: '#/components/parameters/indices.rollover::path.alias'
        - $ref: '#/components/parameters/indices.rollover::path.new_index'
        - $ref: '#/components/parameters/indices.rollover::query.timeout'
        - $ref: '#/components/parameters/indices.rollover::query.dry_run'
        - $ref: '#/components/parameters/indices.rollover::query.master_timeout'
        - $ref: '#/components/parameters/indices.rollover::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.rollover::query.wait_for_active_shards'
      requestBody:
        $ref: '#/components/requestBodies/indices.rollover'
      responses:
        '200':
          $ref: '#/components/responses/indices.rollover@200'
  /{index}:
    delete:
      operationId: indices.delete.0
      x-operation-group: indices.delete
      x-version-added: '1.0'
      description: Deletes an index.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/delete-index/
      parameters:
        - $ref: '#/components/parameters/indices.delete::path.index'
        - $ref: '#/components/parameters/indices.delete::query.timeout'
        - $ref: '#/components/parameters/indices.delete::query.master_timeout'
        - $ref: '#/components/parameters/indices.delete::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.delete::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.delete::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.delete::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.delete@200'
    get:
      operationId: indices.get.0
      x-operation-group: indices.get
      x-version-added: '1.0'
      description: Returns information about one or more indices.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/get-index/
      parameters:
        - $ref: '#/components/parameters/indices.get::path.index'
        - $ref: '#/components/parameters/indices.get::query.local'
        - $ref: '#/components/parameters/indices.get::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get::query.flat_settings'
        - $ref: '#/components/parameters/indices.get::query.include_defaults'
        - $ref: '#/components/parameters/indices.get::query.master_timeout'
        - $ref: '#/components/parameters/indices.get::query.cluster_manager_timeout'
      responses:
        '200':
          $ref: '#/components/responses/indices.get@200'
    head:
      operationId: indices.exists.0
      x-operation-group: indices.exists
      x-version-added: '1.0'
      description: Returns information about whether a particular index exists.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/exists/
      parameters:
        - $ref: '#/components/parameters/indices.exists::path.index'
        - $ref: '#/components/parameters/indices.exists::query.local'
        - $ref: '#/components/parameters/indices.exists::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.exists::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.exists::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.exists::query.flat_settings'
        - $ref: '#/components/parameters/indices.exists::query.include_defaults'
      responses:
        '200':
          $ref: '#/components/responses/indices.exists@200'
    put:
      operationId: indices.create.0
      x-operation-group: indices.create
      x-version-added: '1.0'
      description: Creates an index with optional settings and mappings.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/create-index/
      parameters:
        - $ref: '#/components/parameters/indices.create::path.index'
        - $ref: '#/components/parameters/indices.create::query.wait_for_active_shards'
        - $ref: '#/components/parameters/indices.create::query.timeout'
        - $ref: '#/components/parameters/indices.create::query.master_timeout'
        - $ref: '#/components/parameters/indices.create::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.create'
      responses:
        '200':
          $ref: '#/components/responses/indices.create@200'
  /{index}/_alias:
    get:
      operationId: indices.get_alias.2
      x-operation-group: indices.get_alias
      x-version-added: '1.0'
      description: Returns an alias.
      parameters:
        - $ref: '#/components/parameters/indices.get_alias::path.index'
        - $ref: '#/components/parameters/indices.get_alias::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_alias::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_alias::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_alias::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_alias@200'
  /{index}/_alias/{name}:
    delete:
      operationId: indices.delete_alias.0
      x-operation-group: indices.delete_alias
      x-version-added: '1.0'
      description: Deletes an alias.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-alias/#delete-aliases
      parameters:
        - $ref: '#/components/parameters/indices.delete_alias::path.index'
        - $ref: '#/components/parameters/indices.delete_alias::path.name'
        - $ref: '#/components/parameters/indices.delete_alias::query.timeout'
        - $ref: '#/components/parameters/indices.delete_alias::query.master_timeout'
        - $ref: '#/components/parameters/indices.delete_alias::query.cluster_manager_timeout'
      responses:
        '200':
          $ref: '#/components/responses/indices.delete_alias@200'
    get:
      operationId: indices.get_alias.3
      x-operation-group: indices.get_alias
      x-version-added: '1.0'
      description: Returns an alias.
      parameters:
        - $ref: '#/components/parameters/indices.get_alias::path.index'
        - $ref: '#/components/parameters/indices.get_alias::path.name'
        - $ref: '#/components/parameters/indices.get_alias::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_alias::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_alias::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_alias::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_alias@200'
    head:
      operationId: indices.exists_alias.1
      x-operation-group: indices.exists_alias
      x-version-added: '1.0'
      description: Returns information about whether a particular alias exists.
      parameters:
        - $ref: '#/components/parameters/indices.exists_alias::path.index'
        - $ref: '#/components/parameters/indices.exists_alias::path.name'
        - $ref: '#/components/parameters/indices.exists_alias::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.exists_alias::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.exists_alias::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.exists_alias::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.exists_alias@200'
    post:
      operationId: indices.put_alias.0
      x-operation-group: indices.put_alias
      x-version-added: '1.0'
      description: Creates or updates an alias.
      parameters:
        - $ref: '#/components/parameters/indices.put_alias::path.index'
        - $ref: '#/components/parameters/indices.put_alias::path.name'
        - $ref: '#/components/parameters/indices.put_alias::query.timeout'
        - $ref: '#/components/parameters/indices.put_alias::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_alias::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_alias'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_alias@200'
    put:
      operationId: indices.put_alias.1
      x-operation-group: indices.put_alias
      x-version-added: '1.0'
      description: Creates or updates an alias.
      externalDocs:
        url: https://opensearch.org/docs/latest/im-plugin/index-alias/#create-aliases
      parameters:
        - $ref: '#/components/parameters/indices.put_alias::path.index'
        - $ref: '#/components/parameters/indices.put_alias::path.name'
        - $ref: '#/components/parameters/indices.put_alias::query.timeout'
        - $ref: '#/components/parameters/indices.put_alias::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_alias::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_alias'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_alias@200'
  /{index}/_aliases/{name}:
    delete:
      operationId: indices.delete_alias.1
      x-operation-group: indices.delete_alias
      x-version-added: '1.0'
      description: Deletes an alias.
      parameters:
        - $ref: '#/components/parameters/indices.delete_alias::path.index'
        - $ref: '#/components/parameters/indices.delete_alias::path.name'
        - $ref: '#/components/parameters/indices.delete_alias::query.timeout'
        - $ref: '#/components/parameters/indices.delete_alias::query.master_timeout'
        - $ref: '#/components/parameters/indices.delete_alias::query.cluster_manager_timeout'
      responses:
        '200':
          $ref: '#/components/responses/indices.delete_alias@200'
    post:
      operationId: indices.put_alias.2
      x-operation-group: indices.put_alias
      x-version-added: '1.0'
      description: Creates or updates an alias.
      parameters:
        - $ref: '#/components/parameters/indices.put_alias::path.index'
        - $ref: '#/components/parameters/indices.put_alias::path.name'
        - $ref: '#/components/parameters/indices.put_alias::query.timeout'
        - $ref: '#/components/parameters/indices.put_alias::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_alias::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_alias'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_alias@200'
    put:
      operationId: indices.put_alias.3
      x-operation-group: indices.put_alias
      x-version-added: '1.0'
      description: Creates or updates an alias.
      parameters:
        - $ref: '#/components/parameters/indices.put_alias::path.index'
        - $ref: '#/components/parameters/indices.put_alias::path.name'
        - $ref: '#/components/parameters/indices.put_alias::query.timeout'
        - $ref: '#/components/parameters/indices.put_alias::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_alias::query.cluster_manager_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_alias'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_alias@200'
  /{index}/_analyze:
    get:
      operationId: indices.analyze.2
      x-operation-group: indices.analyze
      x-version-added: '1.0'
      description: Performs the analysis process on a text and return the tokens breakdown of the text.
      parameters:
        - $ref: '#/components/parameters/indices.analyze::path.index'
        - $ref: '#/components/parameters/indices.analyze::query.index'
      requestBody:
        $ref: '#/components/requestBodies/indices.analyze'
      responses:
        '200':
          $ref: '#/components/responses/indices.analyze@200'
    post:
      operationId: indices.analyze.3
      x-operation-group: indices.analyze
      x-version-added: '1.0'
      description: Performs the analysis process on a text and return the tokens breakdown of the text.
      parameters:
        - $ref: '#/components/parameters/indices.analyze::path.index'
        - $ref: '#/components/parameters/indices.analyze::query.index'
      requestBody:
        $ref: '#/components/requestBodies/indices.analyze'
      responses:
        '200':
          $ref: '#/components/responses/indices.analyze@200'
  /{index}/_block/{block}:
    put:
      operationId: indices.add_block.0
      x-operation-group: indices.add_block
      x-version-added: '1.0'
      description: Adds a block to an index.
      externalDocs:
        url: https://opensearch.org/docs/latest
      parameters:
        - $ref: '#/components/parameters/indices.add_block::path.index'
        - $ref: '#/components/parameters/indices.add_block::path.block'
        - $ref: '#/components/parameters/indices.add_block::query.timeout'
        - $ref: '#/components/parameters/indices.add_block::query.master_timeout'
        - $ref: '#/components/parameters/indices.add_block::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.add_block::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.add_block::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.add_block::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.add_block@200'
  /{index}/_cache/clear:
    post:
      operationId: indices.clear_cache.1
      x-operation-group: indices.clear_cache
      x-version-added: '1.0'
      description: Clears all or specific caches for one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.clear_cache::path.index'
        - $ref: '#/components/parameters/indices.clear_cache::query.fielddata'
        - $ref: '#/components/parameters/indices.clear_cache::query.fields'
        - $ref: '#/components/parameters/indices.clear_cache::query.query'
        - $ref: '#/components/parameters/indices.clear_cache::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.clear_cache::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.clear_cache::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.clear_cache::query.index'
        - $ref: '#/components/parameters/indices.clear_cache::query.request'
      responses:
        '200':
          $ref: '#/components/responses/indices.clear_cache@200'
  /{index}/_clone/{target}:
    post:
      operationId: indices.clone.0
      x-operation-group: indices.clone
      x-version-added: '1.0'
      description: Clones an index.
      parameters:
        - $ref: '#/components/parameters/indices.clone::path.index'
        - $ref: '#/components/parameters/indices.clone::path.target'
        - $ref: '#/components/parameters/indices.clone::query.timeout'
        - $ref: '#/components/parameters/indices.clone::query.master_timeout'
        - $ref: '#/components/parameters/indices.clone::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.clone::query.wait_for_active_shards'
        - $ref: '#/components/parameters/indices.clone::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.clone::query.task_execution_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.clone'
      responses:
        '200':
          $ref: '#/components/responses/indices.clone@200'
    put:
      operationId: indices.clone.1
      x-operation-group: indices.clone
      x-version-added: '1.0'
      description: Clones an index.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/clone/
      parameters:
        - $ref: '#/components/parameters/indices.clone::path.index'
        - $ref: '#/components/parameters/indices.clone::path.target'
        - $ref: '#/components/parameters/indices.clone::query.timeout'
        - $ref: '#/components/parameters/indices.clone::query.master_timeout'
        - $ref: '#/components/parameters/indices.clone::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.clone::query.wait_for_active_shards'
        - $ref: '#/components/parameters/indices.clone::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.clone::query.task_execution_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.clone'
      responses:
        '200':
          $ref: '#/components/responses/indices.clone@200'
  /{index}/_close:
    post:
      operationId: indices.close.0
      x-operation-group: indices.close
      x-version-added: '1.0'
      description: Closes an index.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/close-index/
      parameters:
        - $ref: '#/components/parameters/indices.close::path.index'
        - $ref: '#/components/parameters/indices.close::query.timeout'
        - $ref: '#/components/parameters/indices.close::query.master_timeout'
        - $ref: '#/components/parameters/indices.close::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.close::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.close::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.close::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.close::query.wait_for_active_shards'
      responses:
        '200':
          $ref: '#/components/responses/indices.close@200'
  /{index}/_flush:
    get:
      operationId: indices.flush.2
      x-operation-group: indices.flush
      x-version-added: '1.0'
      description: Performs the flush operation on one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.flush::path.index'
        - $ref: '#/components/parameters/indices.flush::query.force'
        - $ref: '#/components/parameters/indices.flush::query.wait_if_ongoing'
        - $ref: '#/components/parameters/indices.flush::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.flush::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.flush::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.flush@200'
    post:
      operationId: indices.flush.3
      x-operation-group: indices.flush
      x-version-added: '1.0'
      description: Performs the flush operation on one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.flush::path.index'
        - $ref: '#/components/parameters/indices.flush::query.force'
        - $ref: '#/components/parameters/indices.flush::query.wait_if_ongoing'
        - $ref: '#/components/parameters/indices.flush::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.flush::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.flush::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.flush@200'
  /{index}/_forcemerge:
    post:
      operationId: indices.forcemerge.1
      x-operation-group: indices.forcemerge
      x-version-added: '1.0'
      description: Performs the force merge operation on one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.forcemerge::path.index'
        - $ref: '#/components/parameters/indices.forcemerge::query.flush'
        - $ref: '#/components/parameters/indices.forcemerge::query.primary_only'
        - $ref: '#/components/parameters/indices.forcemerge::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.forcemerge::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.forcemerge::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.forcemerge::query.max_num_segments'
        - $ref: '#/components/parameters/indices.forcemerge::query.only_expunge_deletes'
        - $ref: '#/components/parameters/indices.forcemerge::query.wait_for_completion'
      responses:
        '200':
          $ref: '#/components/responses/indices.forcemerge@200'
  /{index}/_mapping:
    get:
      operationId: indices.get_mapping.1
      x-operation-group: indices.get_mapping
      x-version-added: '1.0'
      description: Returns mappings for one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.get_mapping::path.index'
        - $ref: '#/components/parameters/indices.get_mapping::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_mapping::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_mapping::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_mapping::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_mapping::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_mapping::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_mapping@200'
    post:
      operationId: indices.put_mapping.0
      x-operation-group: indices.put_mapping
      x-version-added: '1.0'
      description: Updates the index mappings.
      parameters:
        - $ref: '#/components/parameters/indices.put_mapping::path.index'
        - $ref: '#/components/parameters/indices.put_mapping::query.timeout'
        - $ref: '#/components/parameters/indices.put_mapping::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_mapping::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.put_mapping::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.put_mapping::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.put_mapping::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.put_mapping::query.write_index_only'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_mapping'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_mapping@200'
    put:
      operationId: indices.put_mapping.1
      x-operation-group: indices.put_mapping
      x-version-added: '1.0'
      description: Updates the index mappings.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/put-mapping/
      parameters:
        - $ref: '#/components/parameters/indices.put_mapping::path.index'
        - $ref: '#/components/parameters/indices.put_mapping::query.timeout'
        - $ref: '#/components/parameters/indices.put_mapping::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_mapping::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.put_mapping::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.put_mapping::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.put_mapping::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.put_mapping::query.write_index_only'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_mapping'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_mapping@200'
  /{index}/_mapping/field/{fields}:
    get:
      operationId: indices.get_field_mapping.1
      x-operation-group: indices.get_field_mapping
      x-version-added: '1.0'
      description: Returns mapping for one or more fields.
      parameters:
        - $ref: '#/components/parameters/indices.get_field_mapping::path.index'
        - $ref: '#/components/parameters/indices.get_field_mapping::path.fields'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.include_defaults'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_field_mapping::query.local'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_field_mapping@200'
  /{index}/_open:
    post:
      operationId: indices.open.0
      x-operation-group: indices.open
      x-version-added: '1.0'
      description: Opens an index.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/open-index/
      parameters:
        - $ref: '#/components/parameters/indices.open::path.index'
        - $ref: '#/components/parameters/indices.open::query.timeout'
        - $ref: '#/components/parameters/indices.open::query.master_timeout'
        - $ref: '#/components/parameters/indices.open::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.open::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.open::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.open::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.open::query.wait_for_active_shards'
        - $ref: '#/components/parameters/indices.open::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.open::query.task_execution_timeout'
      responses:
        '200':
          $ref: '#/components/responses/indices.open@200'
  /{index}/_recovery:
    get:
      operationId: indices.recovery.1
      x-operation-group: indices.recovery
      x-version-added: '1.0'
      description: Returns information about ongoing index shard recoveries.
      parameters:
        - $ref: '#/components/parameters/indices.recovery::path.index'
        - $ref: '#/components/parameters/indices.recovery::query.detailed'
        - $ref: '#/components/parameters/indices.recovery::query.active_only'
      responses:
        '200':
          $ref: '#/components/responses/indices.recovery@200'
  /{index}/_refresh:
    get:
      operationId: indices.refresh.2
      x-operation-group: indices.refresh
      x-version-added: '1.0'
      description: Performs the refresh operation in one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.refresh::path.index'
        - $ref: '#/components/parameters/indices.refresh::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.refresh::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.refresh::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.refresh@200'
    post:
      operationId: indices.refresh.3
      x-operation-group: indices.refresh
      x-version-added: '1.0'
      description: Performs the refresh operation in one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.refresh::path.index'
        - $ref: '#/components/parameters/indices.refresh::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.refresh::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.refresh::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.refresh@200'
  /{index}/_segments:
    get:
      operationId: indices.segments.1
      x-operation-group: indices.segments
      x-version-added: '1.0'
      description: Provides low-level information about segments in a Lucene index.
      parameters:
        - $ref: '#/components/parameters/indices.segments::path.index'
        - $ref: '#/components/parameters/indices.segments::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.segments::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.segments::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.segments::query.verbose'
      responses:
        '200':
          $ref: '#/components/responses/indices.segments@200'
  /{index}/_settings:
    get:
      operationId: indices.get_settings.2
      x-operation-group: indices.get_settings
      x-version-added: '1.0'
      description: Returns settings for one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.get_settings::path.index'
        - $ref: '#/components/parameters/indices.get_settings::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_settings::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_settings::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_settings::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_settings::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_settings::query.flat_settings'
        - $ref: '#/components/parameters/indices.get_settings::query.local'
        - $ref: '#/components/parameters/indices.get_settings::query.include_defaults'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_settings@200'
    put:
      operationId: indices.put_settings.1
      x-operation-group: indices.put_settings
      x-version-added: '1.0'
      description: Updates the index settings.
      parameters:
        - $ref: '#/components/parameters/indices.put_settings::path.index'
        - $ref: '#/components/parameters/indices.put_settings::query.master_timeout'
        - $ref: '#/components/parameters/indices.put_settings::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.put_settings::query.timeout'
        - $ref: '#/components/parameters/indices.put_settings::query.preserve_existing'
        - $ref: '#/components/parameters/indices.put_settings::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.put_settings::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.put_settings::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.put_settings::query.flat_settings'
      requestBody:
        $ref: '#/components/requestBodies/indices.put_settings'
      responses:
        '200':
          $ref: '#/components/responses/indices.put_settings@200'
  /{index}/_settings/{name}:
    get:
      operationId: indices.get_settings.3
      x-operation-group: indices.get_settings
      x-version-added: '1.0'
      description: Returns settings for one or more indices.
      parameters:
        - $ref: '#/components/parameters/indices.get_settings::path.index'
        - $ref: '#/components/parameters/indices.get_settings::path.name'
        - $ref: '#/components/parameters/indices.get_settings::query.master_timeout'
        - $ref: '#/components/parameters/indices.get_settings::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.get_settings::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_settings::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_settings::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.get_settings::query.flat_settings'
        - $ref: '#/components/parameters/indices.get_settings::query.local'
        - $ref: '#/components/parameters/indices.get_settings::query.include_defaults'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_settings@200'
  /{index}/_shard_stores:
    get:
      operationId: indices.shard_stores.1
      x-operation-group: indices.shard_stores
      x-version-added: '1.0'
      description: Provides store information for shard copies of indices.
      parameters:
        - $ref: '#/components/parameters/indices.shard_stores::path.index'
        - $ref: '#/components/parameters/indices.shard_stores::query.status'
        - $ref: '#/components/parameters/indices.shard_stores::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.shard_stores::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.shard_stores::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.shard_stores@200'
  /{index}/_shrink/{target}:
    post:
      operationId: indices.shrink.0
      x-operation-group: indices.shrink
      x-version-added: '1.0'
      description: Allow to shrink an existing index into a new index with fewer primary shards.
      parameters:
        - $ref: '#/components/parameters/indices.shrink::path.index'
        - $ref: '#/components/parameters/indices.shrink::path.target'
        - $ref: '#/components/parameters/indices.shrink::query.copy_settings'
        - $ref: '#/components/parameters/indices.shrink::query.timeout'
        - $ref: '#/components/parameters/indices.shrink::query.master_timeout'
        - $ref: '#/components/parameters/indices.shrink::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.shrink::query.wait_for_active_shards'
        - $ref: '#/components/parameters/indices.shrink::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.shrink::query.task_execution_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.shrink'
      responses:
        '200':
          $ref: '#/components/responses/indices.shrink@200'
    put:
      operationId: indices.shrink.1
      x-operation-group: indices.shrink
      x-version-added: '1.0'
      description: Allow to shrink an existing index into a new index with fewer primary shards.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/shrink-index/
      parameters:
        - $ref: '#/components/parameters/indices.shrink::path.index'
        - $ref: '#/components/parameters/indices.shrink::path.target'
        - $ref: '#/components/parameters/indices.shrink::query.copy_settings'
        - $ref: '#/components/parameters/indices.shrink::query.timeout'
        - $ref: '#/components/parameters/indices.shrink::query.master_timeout'
        - $ref: '#/components/parameters/indices.shrink::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.shrink::query.wait_for_active_shards'
        - $ref: '#/components/parameters/indices.shrink::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.shrink::query.task_execution_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.shrink'
      responses:
        '200':
          $ref: '#/components/responses/indices.shrink@200'
  /{index}/_split/{target}:
    post:
      operationId: indices.split.0
      x-operation-group: indices.split
      x-version-added: '1.0'
      description: Allows you to split an existing index into a new index with more primary shards.
      parameters:
        - $ref: '#/components/parameters/indices.split::path.index'
        - $ref: '#/components/parameters/indices.split::path.target'
        - $ref: '#/components/parameters/indices.split::query.copy_settings'
        - $ref: '#/components/parameters/indices.split::query.timeout'
        - $ref: '#/components/parameters/indices.split::query.master_timeout'
        - $ref: '#/components/parameters/indices.split::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.split::query.wait_for_active_shards'
        - $ref: '#/components/parameters/indices.split::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.split::query.task_execution_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.split'
      responses:
        '200':
          $ref: '#/components/responses/indices.split@200'
    put:
      operationId: indices.split.1
      x-operation-group: indices.split
      x-version-added: '1.0'
      description: Allows you to split an existing index into a new index with more primary shards.
      externalDocs:
        url: https://opensearch.org/docs/latest/api-reference/index-apis/split/
      parameters:
        - $ref: '#/components/parameters/indices.split::path.index'
        - $ref: '#/components/parameters/indices.split::path.target'
        - $ref: '#/components/parameters/indices.split::query.copy_settings'
        - $ref: '#/components/parameters/indices.split::query.timeout'
        - $ref: '#/components/parameters/indices.split::query.master_timeout'
        - $ref: '#/components/parameters/indices.split::query.cluster_manager_timeout'
        - $ref: '#/components/parameters/indices.split::query.wait_for_active_shards'
        - $ref: '#/components/parameters/indices.split::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.split::query.task_execution_timeout'
      requestBody:
        $ref: '#/components/requestBodies/indices.split'
      responses:
        '200':
          $ref: '#/components/responses/indices.split@200'
  /{index}/_stats:
    get:
      operationId: indices.stats.2
      x-operation-group: indices.stats
      x-version-added: '1.0'
      description: Provides statistics on operations happening in an index.
      parameters:
        - $ref: '#/components/parameters/indices.stats::path.index'
        - $ref: '#/components/parameters/indices.stats::query.completion_fields'
        - $ref: '#/components/parameters/indices.stats::query.fielddata_fields'
        - $ref: '#/components/parameters/indices.stats::query.fields'
        - $ref: '#/components/parameters/indices.stats::query.groups'
        - $ref: '#/components/parameters/indices.stats::query.level'
        - $ref: '#/components/parameters/indices.stats::query.include_segment_file_sizes'
        - $ref: '#/components/parameters/indices.stats::query.include_unloaded_segments'
        - $ref: '#/components/parameters/indices.stats::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.stats::query.forbid_closed_indices'
      responses:
        '200':
          $ref: '#/components/responses/indices.stats@200'
  /{index}/_stats/{metric}:
    get:
      operationId: indices.stats.3
      x-operation-group: indices.stats
      x-version-added: '1.0'
      description: Provides statistics on operations happening in an index.
      parameters:
        - $ref: '#/components/parameters/indices.stats::path.index'
        - $ref: '#/components/parameters/indices.stats::path.metric'
        - $ref: '#/components/parameters/indices.stats::query.completion_fields'
        - $ref: '#/components/parameters/indices.stats::query.fielddata_fields'
        - $ref: '#/components/parameters/indices.stats::query.fields'
        - $ref: '#/components/parameters/indices.stats::query.groups'
        - $ref: '#/components/parameters/indices.stats::query.level'
        - $ref: '#/components/parameters/indices.stats::query.include_segment_file_sizes'
        - $ref: '#/components/parameters/indices.stats::query.include_unloaded_segments'
        - $ref: '#/components/parameters/indices.stats::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.stats::query.forbid_closed_indices'
      responses:
        '200':
          $ref: '#/components/responses/indices.stats@200'
  /{index}/_upgrade:
    get:
      operationId: indices.get_upgrade.1
      x-operation-group: indices.get_upgrade
      x-version-added: '1.0'
      description: The _upgrade API is no longer useful and will be removed.
      parameters:
        - $ref: '#/components/parameters/indices.get_upgrade::path.index'
        - $ref: '#/components/parameters/indices.get_upgrade::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.get_upgrade::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.get_upgrade::query.expand_wildcards'
      responses:
        '200':
          $ref: '#/components/responses/indices.get_upgrade@200'
    post:
      operationId: indices.upgrade.1
      x-operation-group: indices.upgrade
      x-version-added: '1.0'
      description: The _upgrade API is no longer useful and will be removed.
      parameters:
        - $ref: '#/components/parameters/indices.upgrade::path.index'
        - $ref: '#/components/parameters/indices.upgrade::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.upgrade::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.upgrade::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.upgrade::query.wait_for_completion'
        - $ref: '#/components/parameters/indices.upgrade::query.only_ancient_segments'
      responses:
        '200':
          $ref: '#/components/responses/indices.upgrade@200'
  /{index}/_validate/query:
    get:
      operationId: indices.validate_query.2
      x-operation-group: indices.validate_query
      x-version-added: '1.0'
      description: Allows a user to validate a potentially expensive query without executing it.
      parameters:
        - $ref: '#/components/parameters/indices.validate_query::path.index'
        - $ref: '#/components/parameters/indices.validate_query::query.explain'
        - $ref: '#/components/parameters/indices.validate_query::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.validate_query::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.validate_query::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.validate_query::query.q'
        - $ref: '#/components/parameters/indices.validate_query::query.analyzer'
        - $ref: '#/components/parameters/indices.validate_query::query.analyze_wildcard'
        - $ref: '#/components/parameters/indices.validate_query::query.default_operator'
        - $ref: '#/components/parameters/indices.validate_query::query.df'
        - $ref: '#/components/parameters/indices.validate_query::query.lenient'
        - $ref: '#/components/parameters/indices.validate_query::query.rewrite'
        - $ref: '#/components/parameters/indices.validate_query::query.all_shards'
      requestBody:
        $ref: '#/components/requestBodies/indices.validate_query'
      responses:
        '200':
          $ref: '#/components/responses/indices.validate_query@200'
    post:
      operationId: indices.validate_query.3
      x-operation-group: indices.validate_query
      x-version-added: '1.0'
      description: Allows a user to validate a potentially expensive query without executing it.
      parameters:
        - $ref: '#/components/parameters/indices.validate_query::path.index'
        - $ref: '#/components/parameters/indices.validate_query::query.explain'
        - $ref: '#/components/parameters/indices.validate_query::query.ignore_unavailable'
        - $ref: '#/components/parameters/indices.validate_query::query.allow_no_indices'
        - $ref: '#/components/parameters/indices.validate_query::query.expand_wildcards'
        - $ref: '#/components/parameters/indices.validate_query::query.q'
        - $ref: '#/components/parameters/indices.validate_query::query.analyzer'
        - $ref: '#/components/parameters/indices.validate_query::query.analyze_wildcard'
        - $ref: '#/components/parameters/indices.validate_query::query.default_operator'
        - $ref: '#/components/parameters/indices.validate_query::query.df'
        - $ref: '#/components/parameters/indices.validate_query::query.lenient'
        - $ref: '#/components/parameters/indices.validate_query::query.rewrite'
        - $ref: '#/components/parameters/indices.validate_query::query.all_shards'
      requestBody:
        $ref: '#/components/requestBodies/indices.validate_query'
      responses:
        '200':
          $ref: '#/components/responses/indices.validate_query@200'
components:
  requestBodies:
    indices.analyze:
      content:
        application/json:
          schema:
            type: object
            properties:
              analyzer:
                description: |-
                  The name of the analyzer that should be applied to the provided `text`.
                  This could be a built-in analyzer, or an analyzer that’s been configured in the index.
                type: string
              attributes:
                description: Array of token attributes used to filter the output of the `explain` parameter.
                type: array
                items:
                  type: string
              char_filter:
                description: Array of character filters used to preprocess characters before the tokenizer.
                type: array
                items:
                  $ref: '../schemas/_common.analysis.yaml#/components/schemas/CharFilter'
              explain:
                description: If `true`, the response includes token attributes and additional details.
                type: boolean
              field:
                $ref: '../schemas/_common.yaml#/components/schemas/Field'
              filter:
                description: Array of token filters used to apply after the tokenizer.
                type: array
                items:
                  $ref: '../schemas/_common.analysis.yaml#/components/schemas/TokenFilter'
              normalizer:
                description: Normalizer to use to convert text into a single token.
                type: string
              text:
                $ref: '../schemas/indices.analyze.yaml#/components/schemas/TextToAnalyze'
              tokenizer:
                $ref: '../schemas/_common.analysis.yaml#/components/schemas/Tokenizer'
            description: Define analyzer/tokenizer parameters and the text on which the analysis should be performed
    indices.clone:
      content:
        application/json:
          schema:
            type: object
            properties:
              aliases:
                description: Aliases for the resulting index.
                type: object
                additionalProperties:
                  $ref: '../schemas/indices._common.yaml#/components/schemas/Alias'
              settings:
                description: Configuration options for the target index.
                type: object
                additionalProperties:
                  type: object
            description: The configuration for the target index (`settings` and `aliases`)
    indices.create:
      content:
        application/json:
          schema:
            type: object
            properties:
              aliases:
                description: Aliases for the index.
                type: object
                additionalProperties:
                  $ref: '../schemas/indices._common.yaml#/components/schemas/Alias'
              mappings:
                $ref: '../schemas/_common.mapping.yaml#/components/schemas/TypeMapping'
              settings:
                $ref: '../schemas/indices._common.yaml#/components/schemas/IndexSettings'
            description: The configuration for the index (`settings` and `mappings`)
    indices.create_data_stream:
      content:
        application/json:
          schema:
            type: object
            description: The data stream definition
    indices.put_alias:
      content:
        application/json:
          schema:
            type: object
            properties:
              filter:
                $ref: '../schemas/_common.query_dsl.yaml#/components/schemas/QueryContainer'
              index_routing:
                $ref: '../schemas/_common.yaml#/components/schemas/Routing'
              is_write_index:
                description: >-
                  If `true`, sets the write index or data stream for the alias.

                  If an alias points to multiple indices or data streams and `is_write_index` isn’t set, the alias rejects write requests.

                  If an index alias points to one index and `is_write_index` isn’t set, the index automatically acts as the write index.

                  Data stream aliases don’t automatically set a write data stream, even if the alias points to one data stream.
                type: boolean
              routing:
                $ref: '../schemas/_common.yaml#/components/schemas/Routing'
              search_routing:
                $ref: '../schemas/_common.yaml#/components/schemas/Routing'
            description: The settings for the alias, such as `routing` or `filter`
    indices.put_index_template:
      content:
        application/json:
          schema:
            type: object
            properties:
              index_patterns:
                $ref: '../schemas/_common.yaml#/components/schemas/Indices'
              composed_of:
                description: >-
                  An ordered list of component template names.

                  Component templates are merged in the order specified, meaning that the last component template specified has the highest precedence.
                type: array
                items:
                  $ref: '../schemas/_common.yaml#/components/schemas/Name'
              template:
                $ref: '../schemas/indices.put_index_template.yaml#/components/schemas/IndexTemplateMapping'
              data_stream:
                $ref: '../schemas/indices._common.yaml#/components/schemas/DataStreamVisibility'
              priority:
                description: |-
                  Priority to determine index template precedence when a new data stream or index is created.
                  The index template with the highest priority is chosen.
                  If no priority is specified the template is treated as though it is of priority 0 (lowest priority).
                  This number is not automatically generated by Opensearch.
                type: number
              version:
                $ref: '../schemas/_common.yaml#/components/schemas/VersionNumber'
              _meta:
                $ref: '../schemas/_common.yaml#/components/schemas/Metadata'
            description: The template definition
      required: true
    indices.put_mapping:
      content:
        application/json:
          schema:
            type: object
            properties:
              date_detection:
                description: Controls whether dynamic date detection is enabled.
                type: boolean
              dynamic:
                $ref: '../schemas/_common.mapping.yaml#/components/schemas/DynamicMapping'
              dynamic_date_formats:
                description: |-
                  If date detection is enabled then new string fields are checked
                  against 'dynamic_date_formats' and if the value matches then
                  a new date field is added instead of string.
                type: array
                items:
                  type: string
              dynamic_templates:
                description: Specify dynamic templates for the mapping.
                oneOf:
                  - type: object
                    additionalProperties:
                      $ref: '../schemas/_common.mapping.yaml#/components/schemas/DynamicTemplate'
                  - type: array
                    items:
                      type: object
                      additionalProperties:
                        $ref: '../schemas/_common.mapping.yaml#/components/schemas/DynamicTemplate'
              _field_names:
                $ref: '../schemas/_common.mapping.yaml#/components/schemas/FieldNamesField'
              _meta:
                $ref: '../schemas/_common.yaml#/components/schemas/Metadata'
              numeric_detection:
                description: Automatically map strings into numeric data types for all fields.
                type: boolean
              properties:
                description: |-
                  Mapping for a field. For new fields, this mapping can include:

                  - Field name
                  - Field data type
                  - Mapping parameters
                type: object
                additionalProperties:
                  $ref: '../schemas/_common.mapping.yaml#/components/schemas/Property'
              _routing:
                $ref: '../schemas/_common.mapping.yaml#/components/schemas/RoutingField'
              _source:
                $ref: '../schemas/_common.mapping.yaml#/components/schemas/SourceField'
              runtime:
                $ref: '../schemas/_common.mapping.yaml#/components/schemas/RuntimeFields'
            description: The mapping definition
      required: true
    indices.put_settings:
      content:
        application/json:
          schema:
            $ref: '../schemas/indices._common.yaml#/components/schemas/IndexSettings'
      required: true
    indices.put_template:
      content:
        application/json:
          schema:
            type: object
            properties:
              aliases:
                description: Aliases for the index.
                type: object
                additionalProperties:
                  $ref: '../schemas/indices._common.yaml#/components/schemas/Alias'
              index_patterns:
                description: |-
                  Array of wildcard expressions used to match the names
                  of indices during creation.
                oneOf:
                  - type: string
                  - type: array
                    items:
                      type: string
              mappings:
                $ref: '../schemas/_common.mapping.yaml#/components/schemas/TypeMapping'
              order:
                description: |-
                  Order in which Opensearch applies this template if index
                  matches multiple templates.

                  Templates with lower 'order' values are merged first. Templates with higher
                  'order' values are merged later, overriding templates with lower values.
                type: number
              settings:
                description: Configuration options for the index.
                type: object
                additionalProperties:
                  type: object
              version:
                $ref: '../schemas/_common.yaml#/components/schemas/VersionNumber'
            description: The template definition
      required: true
    indices.rollover:
      content:
        application/json:
          schema:
            type: object
            properties:
              aliases:
                description: |-
                  Aliases for the target index.
                  Data streams do not support this parameter.
                type: object
                additionalProperties:
                  $ref: '../schemas/indices._common.yaml#/components/schemas/Alias'
              conditions:
                $ref: '../schemas/indices.rollover.yaml#/components/schemas/RolloverConditions'
              mappings:
                $ref: '../schemas/_common.mapping.yaml#/components/schemas/TypeMapping'
              settings:
                description: |-
                  Configuration options for the index.
                  Data streams do not support this parameter.
                type: object
                additionalProperties:
                  type: object
            description: The conditions that needs to be met for executing rollover
    indices.shrink:
      content:
        application/json:
          schema:
            type: object
            properties:
              aliases:
                description: |-
                  The key is the alias name.
                  Index alias names support date math.
                type: object
                additionalProperties:
                  $ref: '../schemas/indices._common.yaml#/components/schemas/Alias'
              settings:
                description: Configuration options for the target index.
                type: object
                additionalProperties:
                  type: object
            description: The configuration for the target index (`settings` and `aliases`)
    indices.simulate_index_template:
      content:
        application/json:
          schema:
            type: object
            properties:
              allow_auto_create:
                description: >-
                  This setting overrides the value of the `action.auto_create_index` cluster setting.

                  If set to `true` in a template, then indices can be automatically created using that template even if auto-creation of indices is disabled via `actions.auto_create_index`.

                  If set to `false`, then indices or data streams matching the template must always be explicitly created, and may never be automatically created.
                type: boolean
              index_patterns:
                $ref: '../schemas/_common.yaml#/components/schemas/Indices'
              composed_of:
                description: >-
                  An ordered list of component template names.

                  Component templates are merged in the order specified, meaning that the last component template specified has the highest precedence.
                type: array
                items:
                  $ref: '../schemas/_common.yaml#/components/schemas/Name'
              template:
                $ref: '../schemas/indices.put_index_template.yaml#/components/schemas/IndexTemplateMapping'
              data_stream:
                $ref: '../schemas/indices._common.yaml#/components/schemas/DataStreamVisibility'
              priority:
                description: |-
                  Priority to determine index template precedence when a new data stream or index is created.
                  The index template with the highest priority is chosen.
                  If no priority is specified the template is treated as though it is of priority 0 (lowest priority).
                  This number is not automatically generated by Opensearch.
                type: number
              version:
                $ref: '../schemas/_common.yaml#/components/schemas/VersionNumber'
              _meta:
                $ref: '../schemas/_common.yaml#/components/schemas/Metadata'
            description: New index template definition, which will be included in the simulation, as if it already exists in the
              system
    indices.simulate_template:
      content:
        application/json:
          schema:
            $ref: '../schemas/indices._common.yaml#/components/schemas/IndexTemplate'
    indices.split:
      content:
        application/json:
          schema:
            type: object
            properties:
              aliases:
                description: Aliases for the resulting index.
                type: object
                additionalProperties:
                  $ref: '../schemas/indices._common.yaml#/components/schemas/Alias'
              settings:
                description: Configuration options for the target index.
                type: object
                additionalProperties:
                  type: object
            description: The configuration for the target index (`settings` and `aliases`)
    indices.update_aliases:
      content:
        application/json:
          schema:
            type: object
            properties:
              actions:
                description: Actions to perform.
                type: array
                items:
                  $ref: '../schemas/indices.update_aliases.yaml#/components/schemas/Action'
            description: The definition of `actions` to perform
      required: true
    indices.validate_query:
      content:
        application/json:
          schema:
            type: object
            properties:
              query:
                $ref: '../schemas/_common.query_dsl.yaml#/components/schemas/QueryContainer'
            description: The query definition specified with the Query DSL
  responses:
    indices.add_block@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              acknowledged:
                type: boolean
              shards_acknowledged:
                type: boolean
              indices:
                type: array
                items:
                  $ref: '../schemas/indices.add_block.yaml#/components/schemas/IndicesBlockStatus'
            required:
              - acknowledged
              - shards_acknowledged
              - indices
    indices.analyze@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              detail:
                $ref: '../schemas/indices.analyze.yaml#/components/schemas/AnalyzeDetail'
              tokens:
                type: array
                items:
                  $ref: '../schemas/indices.analyze.yaml#/components/schemas/AnalyzeToken'
    indices.clear_cache@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/ShardsOperationResponseBase'
    indices.clone@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              acknowledged:
                type: boolean
              index:
                $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
              shards_acknowledged:
                type: boolean
            required:
              - acknowledged
              - index
              - shards_acknowledged
    indices.close@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              acknowledged:
                type: boolean
              indices:
                type: object
                additionalProperties:
                  $ref: '../schemas/indices.close.yaml#/components/schemas/CloseIndexResult'
              shards_acknowledged:
                type: boolean
            required:
              - acknowledged
              - indices
              - shards_acknowledged
    indices.create@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              index:
                $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
              shards_acknowledged:
                type: boolean
              acknowledged:
                type: boolean
            required:
              - index
              - shards_acknowledged
              - acknowledged
    indices.create_data_stream@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.data_streams_stats@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              _shards:
                $ref: '../schemas/_common.yaml#/components/schemas/ShardStatistics'
              backing_indices:
                description: Total number of backing indices for the selected data streams.
                type: number
              data_stream_count:
                description: Total number of selected data streams.
                type: number
              data_streams:
                description: Contains statistics for the selected data streams.
                type: array
                items:
                  $ref: '../schemas/indices.data_streams_stats.yaml#/components/schemas/DataStreamsStatsItem'
              total_store_sizes:
                $ref: '../schemas/_common.yaml#/components/schemas/ByteSize'
              total_store_size_bytes:
                description: Total size, in bytes, of all shards for the selected data streams.
                type: number
            required:
              - _shards
              - backing_indices
              - data_stream_count
              - data_streams
              - total_store_size_bytes
    indices.delete@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/IndicesResponseBase'
    indices.delete_alias@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.delete_data_stream@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.delete_index_template@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.delete_template@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.exists@200:
      description: ''
      content:
        application/json: {}
    indices.exists_alias@200:
      description: ''
      content:
        application/json: {}
    indices.exists_index_template@200:
      description: ''
      content:
        application/json: {}
    indices.exists_template@200:
      description: ''
      content:
        application/json: {}
    indices.flush@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/ShardsOperationResponseBase'
    indices.forcemerge@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/indices.forcemerge._types.yaml#/components/schemas/ForceMergeResponseBody'
    indices.get@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              $ref: '../schemas/indices._common.yaml#/components/schemas/IndexState'
    indices.get_alias@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              $ref: '../schemas/indices.get_alias.yaml#/components/schemas/IndexAliases'
    indices.get_data_stream@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              data_streams:
                type: array
                items:
                  $ref: '../schemas/indices._common.yaml#/components/schemas/DataStream'
            required:
              - data_streams
    indices.get_field_mapping@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              $ref: '../schemas/indices.get_field_mapping.yaml#/components/schemas/TypeFieldMappings'
    indices.get_index_template@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              index_templates:
                type: array
                items:
                  $ref: '../schemas/indices.get_index_template.yaml#/components/schemas/IndexTemplateItem'
            required:
              - index_templates
    indices.get_mapping@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              $ref: '../schemas/indices.get_mapping.yaml#/components/schemas/IndexMappingRecord'
    indices.get_settings@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              $ref: '../schemas/indices._common.yaml#/components/schemas/IndexState'
    indices.get_template@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              $ref: '../schemas/indices._common.yaml#/components/schemas/TemplateMapping'
    indices.get_upgrade@200:
      description: ''
    indices.open@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              acknowledged:
                type: boolean
              shards_acknowledged:
                type: boolean
            required:
              - acknowledged
              - shards_acknowledged
    indices.put_alias@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.put_index_template@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.put_mapping@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/IndicesResponseBase'
    indices.put_settings@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.put_template@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.recovery@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              $ref: '../schemas/indices.recovery.yaml#/components/schemas/RecoveryStatus'
    indices.refresh@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/ShardsOperationResponseBase'
    indices.resolve_index@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              indices:
                type: array
                items:
                  $ref: '../schemas/indices.resolve_index.yaml#/components/schemas/ResolveIndexItem'
              aliases:
                type: array
                items:
                  $ref: '../schemas/indices.resolve_index.yaml#/components/schemas/ResolveIndexAliasItem'
              data_streams:
                type: array
                items:
                  $ref: '../schemas/indices.resolve_index.yaml#/components/schemas/ResolveIndexDataStreamsItem'
            required:
              - indices
              - aliases
              - data_streams
    indices.rollover@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              acknowledged:
                type: boolean
              conditions:
                type: object
                additionalProperties:
                  type: boolean
              dry_run:
                type: boolean
              new_index:
                type: string
              old_index:
                type: string
              rolled_over:
                type: boolean
              shards_acknowledged:
                type: boolean
            required:
              - acknowledged
              - conditions
              - dry_run
              - new_index
              - old_index
              - rolled_over
              - shards_acknowledged
    indices.segments@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              indices:
                type: object
                additionalProperties:
                  $ref: '../schemas/indices.segments.yaml#/components/schemas/IndexSegment'
              _shards:
                $ref: '../schemas/_common.yaml#/components/schemas/ShardStatistics'
            required:
              - indices
              - _shards
    indices.shard_stores@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              indices:
                type: object
                additionalProperties:
                  $ref: '../schemas/indices.shard_stores.yaml#/components/schemas/IndicesShardStores'
            required:
              - indices
    indices.shrink@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              acknowledged:
                type: boolean
              shards_acknowledged:
                type: boolean
              index:
                $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
            required:
              - acknowledged
              - shards_acknowledged
              - index
    indices.simulate_index_template@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
    indices.simulate_template@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              overlapping:
                type: array
                items:
                  $ref: '../schemas/indices.simulate_template.yaml#/components/schemas/Overlapping'
              template:
                $ref: '../schemas/indices.simulate_template.yaml#/components/schemas/Template'
            required:
              - template
    indices.split@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              acknowledged:
                type: boolean
              shards_acknowledged:
                type: boolean
              index:
                $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
            required:
              - acknowledged
              - shards_acknowledged
              - index
    indices.stats@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              indices:
                type: object
                additionalProperties:
                  $ref: '../schemas/indices.stats.yaml#/components/schemas/IndicesStats'
              _shards:
                $ref: '../schemas/_common.yaml#/components/schemas/ShardStatistics'
              _all:
                $ref: '../schemas/indices.stats.yaml#/components/schemas/IndicesStats'
            required:
              - _shards
              - _all
    indices.update_aliases@200:
      description: ''
      content:
        application/json:
          schema:
            $ref: '../schemas/_common.yaml#/components/schemas/AcknowledgedResponseBase'
    indices.upgrade@200:
      description: ''
    indices.validate_query@200:
      description: ''
      content:
        application/json:
          schema:
            type: object
            properties:
              explanations:
                type: array
                items:
                  $ref: '../schemas/indices.validate_query.yaml#/components/schemas/IndicesValidationExplanation'
              _shards:
                $ref: '../schemas/_common.yaml#/components/schemas/ShardStatistics'
              valid:
                type: boolean
              error:
                type: string
            required:
              - valid
  parameters:
    indices.add_block::path.block:
      in: path
      name: block
      description: The block to add (one of read, write, read_only or metadata)
      required: true
      schema:
        $ref: '../schemas/indices.add_block.yaml#/components/schemas/IndicesBlockOptions'
      style: simple
    indices.add_block::path.index:
      in: path
      name: index
      description: A comma separated list of indices to add a block to
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.add_block::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: Whether to ignore if a wildcard indices expression resolves into no concrete indices. (This includes `_all`
        string or when no indices have been specified)
      schema:
        type: boolean
      style: form
    indices.add_block::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.add_block::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: Whether to expand wildcard expression to concrete indices that are open, closed or both.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.add_block::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: Whether specified concrete indices should be ignored when unavailable (missing or closed)
      schema:
        type: boolean
      style: form
    indices.add_block::query.master_timeout:
      in: query
      name: master_timeout
      description: Specify timeout for connection to master
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.add_block::query.timeout:
      in: query
      name: timeout
      description: Explicit operation timeout
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.analyze::path.index:
      in: path
      name: index
      description: >-
        Index used to derive the analyzer.

        If specified, the `analyzer` or field parameter overrides this value.

        If no index is specified or the index does not have a default analyzer, the analyze API uses the standard analyzer.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.analyze::query.index:
      name: index
      in: query
      description: The name of the index to scope the operation.
      schema:
        type: string
        description: The name of the index to scope the operation.
    indices.clear_cache::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.clear_cache::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.clear_cache::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.clear_cache::query.fielddata:
      in: query
      name: fielddata
      description: |-
        If `true`, clears the fields cache.
        Use the `fields` parameter to clear the cache of specific fields only.
      schema:
        type: boolean
      style: form
    indices.clear_cache::query.fields:
      in: query
      name: fields
      description: Comma-separated list of field names used to limit the `fielddata` parameter.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Fields'
      style: form
    indices.clear_cache::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.clear_cache::query.index:
      name: index
      in: query
      description: Comma-separated list of indices; use `_all` or empty string to perform the operation on all indices.
      style: form
      schema:
        type: array
        items:
          type: string
        description: Comma-separated list of indices; use `_all` or empty string to perform the operation on all indices.
      explode: true
    indices.clear_cache::query.query:
      in: query
      name: query
      description: If `true`, clears the query cache.
      schema:
        type: boolean
      style: form
    indices.clear_cache::query.request:
      in: query
      name: request
      description: If `true`, clears the request cache.
      schema:
        type: boolean
      style: form
    indices.clone::path.index:
      in: path
      name: index
      description: Name of the source index to clone.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.clone::path.target:
      in: path
      name: target
      description: Name of the target index to create.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.clone::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.clone::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.clone::query.task_execution_timeout:
      name: task_execution_timeout
      in: query
      description: Explicit task execution timeout, only useful when wait_for_completion is false, defaults to 1h.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
    indices.clone::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.clone::query.wait_for_active_shards:
      in: query
      name: wait_for_active_shards
      description: |-
        The number of shard copies that must be active before proceeding with the operation.
        Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForActiveShards'
      style: form
    indices.clone::query.wait_for_completion:
      name: wait_for_completion
      in: query
      description: Should this request wait until the operation has completed before returning.
      schema:
        type: boolean
        default: true
        description: Should this request wait until the operation has completed before returning.
    indices.close::path.index:
      in: path
      name: index
      description: Comma-separated list or wildcard expression of index names used to limit the request.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.close::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.close::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.close::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.close::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.close::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.close::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.close::query.wait_for_active_shards:
      in: query
      name: wait_for_active_shards
      description: |-
        The number of shard copies that must be active before proceeding with the operation.
        Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForActiveShards'
      style: form
    indices.create::path.index:
      in: path
      name: index
      description: Name of the index you wish to create.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.create::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.create::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.create::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.create::query.wait_for_active_shards:
      in: query
      name: wait_for_active_shards
      description: |-
        The number of shard copies that must be active before proceeding with the operation. 
        Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForActiveShards'
      style: form
    indices.create_data_stream::path.name:
      in: path
      name: name
      description: |-
        Name of the data stream, which must meet the following criteria:
        Lowercase only;
        Cannot include `\`, `/`, `*`, `?`, `"`, `<`, `>`, `|`, `,`, `#`, `:`, or a space character;
        Cannot start with `-`, `_`, `+`, or `.ds-`;
        Cannot be `.` or `..`;
        Cannot be longer than 255 bytes. Multi-byte characters count towards this limit faster.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/DataStreamName'
      style: simple
    indices.data_streams_stats::path.name:
      in: path
      name: name
      description: |-
        Comma-separated list of data streams used to limit the request.
        Wildcard expressions (`*`) are supported.
        To target all data streams in a cluster, omit this parameter or use `*`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.delete::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of indices to delete.
        You cannot specify index aliases.
        By default, this parameter does not support wildcards (`*`) or `_all`.
        To use wildcards or `_all`, set the `action.destructive_requires_name` cluster setting to `false`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.delete::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.delete::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.delete::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.delete::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.delete::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.delete::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.delete_alias::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams or indices used to limit the request.
        Supports wildcards (`*`).
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.delete_alias::path.name:
      in: path
      name: name
      description: |-
        Comma-separated list of aliases to remove.
        Supports wildcards (`*`). To remove all aliases, use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    indices.delete_alias::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.delete_alias::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.delete_alias::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.delete_data_stream::path.name:
      in: path
      name: name
      description: Comma-separated list of data streams to delete. Wildcard (`*`) expressions are supported.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/DataStreamNames'
      style: simple
    indices.delete_index_template::path.name:
      in: path
      name: name
      description: Comma-separated list of index template names used to limit the request. Wildcard (*) expressions are supported.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    indices.delete_index_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.delete_index_template::query.master_timeout:
      in: query
      name: master_timeout
      description: Period to wait for a connection to the master node. If no response is received before the timeout expires,
        the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.delete_index_template::query.timeout:
      in: query
      name: timeout
      description: Period to wait for a response. If no response is received before the timeout expires, the request fails and
        returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.delete_template::path.name:
      in: path
      name: name
      description: |-
        The name of the legacy index template to delete.
        Wildcard (`*`) expressions are supported.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.delete_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.delete_template::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.delete_template::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.exists::path.index:
      in: path
      name: index
      description: Comma-separated list of data streams, indices, and aliases. Supports wildcards (`*`).
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.exists::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.exists::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.exists::query.flat_settings:
      in: query
      name: flat_settings
      description: If `true`, returns settings in flat format.
      schema:
        type: boolean
      style: form
    indices.exists::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.exists::query.include_defaults:
      in: query
      name: include_defaults
      description: If `true`, return all default settings in the response.
      schema:
        type: boolean
      style: form
    indices.exists::query.local:
      in: query
      name: local
      description: If `true`, the request retrieves information from the local node only.
      schema:
        type: boolean
      style: form
    indices.exists_alias::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams or indices used to limit the request. Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.exists_alias::path.name:
      in: path
      name: name
      description: Comma-separated list of aliases to check. Supports wildcards (`*`).
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    indices.exists_alias::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.exists_alias::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.exists_alias::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, requests that include a missing data stream or index in the target indices or data streams
        return an error.
      schema:
        type: boolean
      style: form
    indices.exists_alias::query.local:
      in: query
      name: local
      description: If `true`, the request retrieves information from the local node only.
      schema:
        type: boolean
      style: form
    indices.exists_index_template::path.name:
      in: path
      name: name
      description: Comma-separated list of index template names used to limit the request. Wildcard (*) expressions are supported.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.exists_index_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.exists_index_template::query.flat_settings:
      name: flat_settings
      in: query
      description: Return settings in flat format.
      schema:
        type: boolean
        default: false
        description: Return settings in flat format.
    indices.exists_index_template::query.local:
      name: local
      in: query
      description: Return local information, do not retrieve the state from cluster-manager node.
      schema:
        type: boolean
        default: false
        description: Return local information, do not retrieve the state from cluster-manager node.
    indices.exists_index_template::query.master_timeout:
      in: query
      name: master_timeout
      description: Period to wait for a connection to the master node. If no response is received before the timeout expires,
        the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.exists_template::path.name:
      in: path
      name: name
      description: The comma separated names of the index templates
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    indices.exists_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.exists_template::query.flat_settings:
      in: query
      name: flat_settings
      description: 'Return settings in flat format (default: false)'
      schema:
        type: boolean
      style: form
    indices.exists_template::query.local:
      in: query
      name: local
      description: 'Return local information, do not retrieve the state from master node (default: false)'
      schema:
        type: boolean
      style: form
    indices.exists_template::query.master_timeout:
      in: query
      name: master_timeout
      description: Explicit operation timeout for connection to master node
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.flush::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases to flush.
        Supports wildcards (`*`).
        To flush all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.flush::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.flush::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.flush::query.force:
      in: query
      name: force
      description: If `true`, the request forces a flush even if there are no changes to commit to the index.
      schema:
        type: boolean
      style: form
    indices.flush::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.flush::query.wait_if_ongoing:
      in: query
      name: wait_if_ongoing
      description: |-
        If `true`, the flush operation blocks until execution when another flush operation is running.
        If `false`, Opensearch returns an error if you request a flush when another flush operation is running.
      schema:
        type: boolean
      style: form
    indices.forcemerge::path.index:
      in: path
      name: index
      description: A comma-separated list of index names; use `_all` or empty string to perform the operation on all indices
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.forcemerge::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: Whether to ignore if a wildcard indices expression resolves into no concrete indices. (This includes `_all`
        string or when no indices have been specified)
      schema:
        type: boolean
      style: form
    indices.forcemerge::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: Whether to expand wildcard expression to concrete indices that are open, closed or both.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.forcemerge::query.flush:
      in: query
      name: flush
      description: 'Specify whether the index should be flushed after performing the operation (default: true)'
      schema:
        type: boolean
      style: form
    indices.forcemerge::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: Whether specified concrete indices should be ignored when unavailable (missing or closed)
      schema:
        type: boolean
      style: form
    indices.forcemerge::query.max_num_segments:
      in: query
      name: max_num_segments
      description: 'The number of segments the index should be merged into (default: dynamic)'
      schema:
        type: number
      style: form
    indices.forcemerge::query.only_expunge_deletes:
      in: query
      name: only_expunge_deletes
      description: Specify whether the operation should only expunge deleted documents
      schema:
        type: boolean
      style: form
    indices.forcemerge::query.primary_only:
      name: primary_only
      in: query
      description: Specify whether the operation should only perform on primary shards. Defaults to false.
      schema:
        type: boolean
        default: false
        description: Specify whether the operation should only perform on primary shards. Defaults to false.
    indices.forcemerge::query.wait_for_completion:
      in: query
      name: wait_for_completion
      description: Should the request wait until the force merge is completed.
      schema:
        type: boolean
      style: form
    indices.get::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and index aliases used to limit the request.
        Wildcard expressions (*) are supported.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.get::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: |-
        If false, the request returns an error if any wildcard expression, index alias, or _all value targets only
        missing or closed indices. This behavior applies even if the request targets other open indices. For example,
        a request targeting foo*,bar* returns an error if an index starts with foo but no index starts with bar.
      schema:
        type: boolean
      style: form
    indices.get::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.get::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: |-
        Type of index that wildcard expressions can match. If the request can target data streams, this argument
        determines whether wildcard expressions match hidden data streams. Supports comma-separated values,
        such as open,hidden.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.get::query.flat_settings:
      in: query
      name: flat_settings
      description: If true, returns settings in flat format.
      schema:
        type: boolean
      style: form
    indices.get::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If false, requests that target a missing index return an error.
      schema:
        type: boolean
      style: form
    indices.get::query.include_defaults:
      in: query
      name: include_defaults
      description: If true, return all default settings in the response.
      schema:
        type: boolean
      style: form
    indices.get::query.local:
      in: query
      name: local
      description: If true, the request retrieves information from the local node only. Defaults to false, which means
        information is retrieved from the master node.
      schema:
        type: boolean
      style: form
    indices.get::query.master_timeout:
      in: query
      name: master_timeout
      description: Period to wait for a connection to the master node. If no response is received before the timeout expires,
        the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.get_alias::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams or indices used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.get_alias::path.name:
      in: path
      name: name
      description: |-
        Comma-separated list of aliases to retrieve.
        Supports wildcards (`*`).
        To retrieve all aliases, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    indices.get_alias::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.get_alias::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.get_alias::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.get_alias::query.local:
      in: query
      name: local
      description: If `true`, the request retrieves information from the local node only.
      schema:
        type: boolean
      style: form
    indices.get_data_stream::path.name:
      in: path
      name: name
      description: |-
        Comma-separated list of data stream names used to limit the request.
        Wildcard (`*`) expressions are supported. If omitted, all data streams are returned.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/DataStreamNames'
      style: simple
    indices.get_field_mapping::path.fields:
      in: path
      name: fields
      description: Comma-separated list or wildcard expression of fields used to limit returned information.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Fields'
      style: simple
    indices.get_field_mapping::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.get_field_mapping::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.get_field_mapping::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.get_field_mapping::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.get_field_mapping::query.include_defaults:
      in: query
      name: include_defaults
      description: If `true`, return all default settings in the response.
      schema:
        type: boolean
      style: form
    indices.get_field_mapping::query.local:
      in: query
      name: local
      description: If `true`, the request retrieves information from the local node only.
      schema:
        type: boolean
      style: form
    indices.get_index_template::path.name:
      in: path
      name: name
      description: Comma-separated list of index template names used to limit the request. Wildcard (*) expressions are supported.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.get_index_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.get_index_template::query.flat_settings:
      in: query
      name: flat_settings
      description: If true, returns settings in flat format.
      schema:
        type: boolean
      style: form
    indices.get_index_template::query.local:
      in: query
      name: local
      description: If true, the request retrieves information from the local node only. Defaults to false, which means
        information is retrieved from the master node.
      schema:
        type: boolean
      style: form
    indices.get_index_template::query.master_timeout:
      in: query
      name: master_timeout
      description: Period to wait for a connection to the master node. If no response is received before the timeout expires,
        the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.get_mapping::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.get_mapping::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.get_mapping::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.get_mapping::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.get_mapping::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.get_mapping::query.local:
      in: query
      name: local
      description: If `true`, the request retrieves information from the local node only.
      schema:
        type: boolean
      style: form
    indices.get_mapping::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.get_settings::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit
        the request. Supports wildcards (`*`). To target all data streams and
        indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.get_settings::path.name:
      in: path
      name: name
      description: Comma-separated list or wildcard expression of settings to retrieve.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    indices.get_settings::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: |-
        If `false`, the request returns an error if any wildcard expression, index
        alias, or `_all` value targets only missing or closed indices. This
        behavior applies even if the request targets other open indices. For
        example, a request targeting `foo*,bar*` returns an error if an index
        starts with foo but no index starts with `bar`.
      schema:
        type: boolean
      style: form
    indices.get_settings::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.get_settings::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.get_settings::query.flat_settings:
      in: query
      name: flat_settings
      description: If `true`, returns settings in flat format.
      schema:
        type: boolean
      style: form
    indices.get_settings::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.get_settings::query.include_defaults:
      in: query
      name: include_defaults
      description: If `true`, return all default settings in the response.
      schema:
        type: boolean
      style: form
    indices.get_settings::query.local:
      in: query
      name: local
      description: |-
        If `true`, the request retrieves information from the local node only. If
        `false`, information is retrieved from the master node.
      schema:
        type: boolean
      style: form
    indices.get_settings::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node. If no response is
        received before the timeout expires, the request fails and returns an
        error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.get_template::path.name:
      in: path
      name: name
      description: |-
        Comma-separated list of index template names used to limit the request.
        Wildcard (`*`) expressions are supported.
        To return all index templates, omit this parameter or use a value of `_all` or `*`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    indices.get_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.get_template::query.flat_settings:
      in: query
      name: flat_settings
      description: If `true`, returns settings in flat format.
      schema:
        type: boolean
      style: form
    indices.get_template::query.local:
      in: query
      name: local
      description: If `true`, the request retrieves information from the local node only.
      schema:
        type: boolean
      style: form
    indices.get_template::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.get_upgrade::path.index:
      name: index
      in: path
      description: Comma-separated list of indices; use `_all` or empty string to perform the operation on all indices.
      schema:
        type: string
        pattern: ^(?!_|template|query|field|point|clear|usage|stats|hot|reload|painless).+$
        description: Comma-separated list of indices; use `_all` or empty string to perform the operation on all indices.
        x-data-type: array
      required: true
    indices.get_upgrade::query.allow_no_indices:
      name: allow_no_indices
      in: query
      description: Whether to ignore if a wildcard indices expression resolves into no concrete indices. (This includes `_all`
        string or when no indices have been specified).
      schema:
        type: boolean
        description: Whether to ignore if a wildcard indices expression resolves into no concrete indices. (This includes `_all`
          string or when no indices have been specified).
    indices.get_upgrade::query.expand_wildcards:
      name: expand_wildcards
      in: query
      description: Whether to expand wildcard expression to concrete indices that are open, closed or both.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
    indices.get_upgrade::query.ignore_unavailable:
      name: ignore_unavailable
      in: query
      description: Whether specified concrete indices should be ignored when unavailable (missing or closed).
      schema:
        type: boolean
        description: Whether specified concrete indices should be ignored when unavailable (missing or closed).
    indices.open::path.index:
      in: path
      name: index
      description: >-
        Comma-separated list of data streams, indices, and aliases used to limit the request.

        Supports wildcards (`*`).

        By default, you must explicitly name the indices you using to limit the request.

        To limit a request using `_all`, `*`, or other wildcard expressions, change the `action.destructive_requires_name` setting to false.

        You can update this setting in the `opensearch.yml` file or using the cluster update settings API.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.open::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.open::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.open::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.open::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.open::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.open::query.task_execution_timeout:
      name: task_execution_timeout
      in: query
      description: Explicit task execution timeout, only useful when wait_for_completion is false, defaults to 1h.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
    indices.open::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.open::query.wait_for_active_shards:
      in: query
      name: wait_for_active_shards
      description: |-
        The number of shard copies that must be active before proceeding with the operation.
        Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForActiveShards'
      style: form
    indices.open::query.wait_for_completion:
      name: wait_for_completion
      in: query
      description: Should this request wait until the operation has completed before returning.
      schema:
        type: boolean
        default: true
        description: Should this request wait until the operation has completed before returning.
    indices.put_alias::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams or indices to add.
        Supports wildcards (`*`).
        Wildcard patterns that match both data streams and indices return an error.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.put_alias::path.name:
      in: path
      name: name
      description: |-
        Alias to update.
        If the alias doesn’t exist, the request creates it.
        Index alias names support date math.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.put_alias::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.put_alias::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.put_alias::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.put_index_template::path.name:
      in: path
      name: name
      description: Index or template name
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.put_index_template::query.cause:
      name: cause
      in: query
      description: User defined reason for creating/updating the index template.
      schema:
        type: string
        default: 'false'
        description: User defined reason for creating/updating the index template.
    indices.put_index_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.put_index_template::query.create:
      in: query
      name: create
      description: If `true`, this request cannot replace or update existing index templates.
      schema:
        type: boolean
      style: form
    indices.put_index_template::query.master_timeout:
      name: master_timeout
      in: query
      description: Operation timeout for connection to master node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
      deprecated: true
    indices.put_mapping::path.index:
      in: path
      name: index
      description: A comma-separated list of index names the mapping should be added to (supports wildcards); use `_all` or
        omit to add the mapping on all indices.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.put_mapping::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.put_mapping::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.put_mapping::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.put_mapping::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.put_mapping::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.put_mapping::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.put_mapping::query.write_index_only:
      in: query
      name: write_index_only
      description: If `true`, the mappings are applied only to the current write index for the target.
      schema:
        type: boolean
      style: form
    indices.put_settings::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit
        the request. Supports wildcards (`*`). To target all data streams and
        indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.put_settings::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: |-
        If `false`, the request returns an error if any wildcard expression, index
        alias, or `_all` value targets only missing or closed indices. This
        behavior applies even if the request targets other open indices. For
        example, a request targeting `foo*,bar*` returns an error if an index
        starts with `foo` but no index starts with `bar`.
      schema:
        type: boolean
      style: form
    indices.put_settings::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.put_settings::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: |-
        Type of index that wildcard patterns can match. If the request can target
        data streams, this argument determines whether wildcard expressions match
        hidden data streams. Supports comma-separated values, such as
        `open,hidden`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.put_settings::query.flat_settings:
      in: query
      name: flat_settings
      description: If `true`, returns settings in flat format.
      schema:
        type: boolean
      style: form
    indices.put_settings::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `true`, returns settings in flat format.
      schema:
        type: boolean
      style: form
    indices.put_settings::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node. If no response is
        received before the timeout expires, the request fails and returns an
        error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.put_settings::query.preserve_existing:
      in: query
      name: preserve_existing
      description: If `true`, existing index settings remain unchanged.
      schema:
        type: boolean
      style: form
    indices.put_settings::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response. If no response is received before the
         timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.put_template::path.name:
      in: path
      name: name
      description: The name of the template
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.put_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.put_template::query.create:
      in: query
      name: create
      description: If true, this request cannot replace or update existing index templates.
      schema:
        type: boolean
      style: form
    indices.put_template::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node. If no response is
        received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.put_template::query.order:
      in: query
      name: order
      description: |-
        Order in which Opensearch applies this template if index
        matches multiple templates.

        Templates with lower 'order' values are merged first. Templates with higher
        'order' values are merged later, overriding templates with lower values.
      schema:
        type: number
      style: form
    indices.recovery::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.recovery::query.active_only:
      in: query
      name: active_only
      description: If `true`, the response only includes ongoing shard recoveries.
      schema:
        type: boolean
      style: form
    indices.recovery::query.detailed:
      in: query
      name: detailed
      description: If `true`, the response includes detailed information about shard recoveries.
      schema:
        type: boolean
      style: form
    indices.refresh::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.refresh::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.refresh::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.refresh::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.resolve_index::path.name:
      in: path
      name: name
      description: |-
        Comma-separated name(s) or index pattern(s) of the indices, aliases, and data streams to resolve.
        Resources on remote clusters can be specified using the `<cluster>`:`<name>` syntax.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Names'
      style: simple
    indices.resolve_index::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.rollover::path.alias:
      in: path
      name: alias
      description: Name of the data stream or index alias to roll over.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexAlias'
      style: simple
    indices.rollover::path.new_index:
      in: path
      name: new_index
      description: |-
        Name of the index to create.
        Supports date math.
        Data streams do not support this parameter.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.rollover::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.rollover::query.dry_run:
      in: query
      name: dry_run
      description: If `true`, checks whether the current index satisfies the specified conditions but does not perform a rollover.
      schema:
        type: boolean
      style: form
    indices.rollover::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.rollover::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.rollover::query.wait_for_active_shards:
      in: query
      name: wait_for_active_shards
      description: |-
        The number of shard copies that must be active before proceeding with the operation.
        Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForActiveShards'
      style: form
    indices.segments::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases used to limit the request.
        Supports wildcards (`*`).
        To target all data streams and indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.segments::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.segments::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.segments::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.segments::query.verbose:
      in: query
      name: verbose
      description: If `true`, the request returns a verbose response.
      schema:
        type: boolean
      style: form
    indices.shard_stores::path.index:
      in: path
      name: index
      description: List of data streams, indices, and aliases used to limit the request.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.shard_stores::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: |-
        If false, the request returns an error if any wildcard expression, index alias, or _all
        value targets only missing or closed indices. This behavior applies even if the request
        targets other open indices.
      schema:
        type: boolean
      style: form
    indices.shard_stores::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: |-
        Type of index that wildcard patterns can match. If the request can target data streams,
        this argument determines whether wildcard expressions match hidden data streams.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.shard_stores::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If true, missing or closed indices are not included in the response.
      schema:
        type: boolean
      style: form
    indices.shard_stores::query.status:
      in: query
      name: status
      description: List of shard health statuses used to limit the request.
      schema:
        oneOf:
          - $ref: '../schemas/indices.shard_stores.yaml#/components/schemas/ShardStoreStatus'
          - type: array
            items:
              $ref: '../schemas/indices.shard_stores.yaml#/components/schemas/ShardStoreStatus'
      style: form
    indices.shrink::path.index:
      in: path
      name: index
      description: Name of the source index to shrink.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.shrink::path.target:
      in: path
      name: target
      description: Name of the target index to create.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.shrink::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.shrink::query.copy_settings:
      name: copy_settings
      in: query
      description: whether or not to copy settings from the source index.
      schema:
        type: boolean
        default: false
        description: whether or not to copy settings from the source index.
    indices.shrink::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.shrink::query.task_execution_timeout:
      name: task_execution_timeout
      in: query
      description: Explicit task execution timeout, only useful when wait_for_completion is false, defaults to 1h.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
    indices.shrink::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.shrink::query.wait_for_active_shards:
      in: query
      name: wait_for_active_shards
      description: |-
        The number of shard copies that must be active before proceeding with the operation.
        Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForActiveShards'
      style: form
    indices.shrink::query.wait_for_completion:
      name: wait_for_completion
      in: query
      description: Should this request wait until the operation has completed before returning.
      schema:
        type: boolean
        default: true
        description: Should this request wait until the operation has completed before returning.
    indices.simulate_index_template::path.name:
      in: path
      name: name
      description: Index or template name to simulate
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.simulate_index_template::query.cause:
      name: cause
      in: query
      description: User defined reason for dry-run creating the new template for simulation purposes.
      schema:
        type: string
        default: 'false'
        description: User defined reason for dry-run creating the new template for simulation purposes.
    indices.simulate_index_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.simulate_index_template::query.create:
      in: query
      name: create
      description: |-
        If `true`, the template passed in the body is only used if no existing
        templates match the same index patterns. If `false`, the simulation uses
        the template with the highest priority. Note that the template is not
        permanently added or updated in either case; it is only used for the
        simulation.
      schema:
        type: boolean
      style: form
    indices.simulate_index_template::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node. If no response is received
        before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.simulate_template::path.name:
      in: path
      name: name
      description: |-
        Name of the index template to simulate. To test a template configuration before you add it to the cluster, omit
        this parameter and specify the template configuration in the request body.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Name'
      style: simple
    indices.simulate_template::query.cause:
      name: cause
      in: query
      description: User defined reason for dry-run creating the new template for simulation purposes.
      schema:
        type: string
        default: 'false'
        description: User defined reason for dry-run creating the new template for simulation purposes.
    indices.simulate_template::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.simulate_template::query.create:
      in: query
      name: create
      description: If true, the template passed in the body is only used if no existing templates match the same index
        patterns. If false, the simulation uses the template with the highest priority. Note that the template is not
        permanently added or updated in either case; it is only used for the simulation.
      schema:
        type: boolean
      style: form
    indices.simulate_template::query.master_timeout:
      in: query
      name: master_timeout
      description: Period to wait for a connection to the master node. If no response is received before the timeout expires,
        the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.split::path.index:
      in: path
      name: index
      description: Name of the source index to split.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.split::path.target:
      in: path
      name: target
      description: Name of the target index to create.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/IndexName'
      style: simple
    indices.split::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.split::query.copy_settings:
      name: copy_settings
      in: query
      description: whether or not to copy settings from the source index.
      schema:
        type: boolean
        default: false
        description: whether or not to copy settings from the source index.
    indices.split::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.split::query.task_execution_timeout:
      name: task_execution_timeout
      in: query
      description: Explicit task execution timeout, only useful when wait_for_completion is false, defaults to 1h.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
    indices.split::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.split::query.wait_for_active_shards:
      in: query
      name: wait_for_active_shards
      description: |-
        The number of shard copies that must be active before proceeding with the operation.
        Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/WaitForActiveShards'
      style: form
    indices.split::query.wait_for_completion:
      name: wait_for_completion
      in: query
      description: Should this request wait until the operation has completed before returning.
      schema:
        type: boolean
        default: true
        description: Should this request wait until the operation has completed before returning.
    indices.stats::path.index:
      in: path
      name: index
      description: A comma-separated list of index names; use `_all` or empty string to perform the operation on all indices
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.stats::path.metric:
      in: path
      name: metric
      description: Limit the information returned the specific metrics.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Metrics'
      style: simple
    indices.stats::query.completion_fields:
      in: query
      name: completion_fields
      description: Comma-separated list or wildcard expressions of fields to include in fielddata and suggest statistics.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Fields'
      style: form
    indices.stats::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: |-
        Type of index that wildcard patterns can match. If the request can target data streams, this argument
        determines whether wildcard expressions match hidden data streams. Supports comma-separated values,
        such as `open,hidden`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.stats::query.fielddata_fields:
      in: query
      name: fielddata_fields
      description: Comma-separated list or wildcard expressions of fields to include in fielddata statistics.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Fields'
      style: form
    indices.stats::query.fields:
      in: query
      name: fields
      description: Comma-separated list or wildcard expressions of fields to include in the statistics.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Fields'
      style: form
    indices.stats::query.forbid_closed_indices:
      in: query
      name: forbid_closed_indices
      description: If true, statistics are not collected from closed indices.
      schema:
        type: boolean
      style: form
    indices.stats::query.groups:
      in: query
      name: groups
      description: Comma-separated list of search groups to include in the search statistics.
      schema:
        oneOf:
          - type: string
          - type: array
            items:
              type: string
      style: form
    indices.stats::query.include_segment_file_sizes:
      in: query
      name: include_segment_file_sizes
      description: If true, the call reports the aggregated disk usage of each one of the Lucene index files (only applies if
        segment stats are requested).
      schema:
        type: boolean
      style: form
    indices.stats::query.include_unloaded_segments:
      in: query
      name: include_unloaded_segments
      description: If true, the response includes information from segments that are not loaded into memory.
      schema:
        type: boolean
      style: form
    indices.stats::query.level:
      in: query
      name: level
      description: Indicates whether statistics are aggregated at the cluster, index, or shard level.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Level'
      style: form
    indices.update_aliases::query.cluster_manager_timeout:
      name: cluster_manager_timeout
      in: query
      description: Operation timeout for connection to cluster-manager node.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      x-version-added: 2.0.0
    indices.update_aliases::query.master_timeout:
      in: query
      name: master_timeout
      description: |-
        Period to wait for a connection to the master node.
        If no response is received before the timeout expires, the request fails and returns an error.
      deprecated: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
      x-version-deprecated: 2.0.0
      x-deprecation-message: To promote inclusive language, use 'cluster_manager_timeout' instead.
    indices.update_aliases::query.timeout:
      in: query
      name: timeout
      description: |-
        Period to wait for a response.
        If no response is received before the timeout expires, the request fails and returns an error.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Duration'
      style: form
    indices.upgrade::path.index:
      name: index
      in: path
      description: Comma-separated list of indices; use `_all` or empty string to perform the operation on all indices.
      schema:
        type: string
        pattern: ^(?!_|template|query|field|point|clear|usage|stats|hot|reload|painless).+$
        description: Comma-separated list of indices; use `_all` or empty string to perform the operation on all indices.
        x-data-type: array
      required: true
    indices.upgrade::query.allow_no_indices:
      name: allow_no_indices
      in: query
      description: Whether to ignore if a wildcard indices expression resolves into no concrete indices. (This includes `_all`
        string or when no indices have been specified).
      schema:
        type: boolean
        description: Whether to ignore if a wildcard indices expression resolves into no concrete indices. (This includes `_all`
          string or when no indices have been specified).
    indices.upgrade::query.expand_wildcards:
      name: expand_wildcards
      in: query
      description: Whether to expand wildcard expression to concrete indices that are open, closed or both.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
    indices.upgrade::query.ignore_unavailable:
      name: ignore_unavailable
      in: query
      description: Whether specified concrete indices should be ignored when unavailable (missing or closed).
      schema:
        type: boolean
        description: Whether specified concrete indices should be ignored when unavailable (missing or closed).
    indices.upgrade::query.only_ancient_segments:
      name: only_ancient_segments
      in: query
      description: If true, only ancient (an older Lucene major release) segments will be upgraded.
      schema:
        type: boolean
        description: If true, only ancient (an older Lucene major release) segments will be upgraded.
    indices.upgrade::query.wait_for_completion:
      name: wait_for_completion
      in: query
      description: Should this request wait until the operation has completed before returning.
      schema:
        type: boolean
        default: false
        description: Should this request wait until the operation has completed before returning.
    indices.validate_query::path.index:
      in: path
      name: index
      description: |-
        Comma-separated list of data streams, indices, and aliases to search.
        Supports wildcards (`*`).
        To search all data streams or indices, omit this parameter or use `*` or `_all`.
      required: true
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/Indices'
      style: simple
    indices.validate_query::query.all_shards:
      in: query
      name: all_shards
      description: If `true`, the validation is executed on all shards instead of one random shard per index.
      schema:
        type: boolean
      style: form
    indices.validate_query::query.allow_no_indices:
      in: query
      name: allow_no_indices
      description: >-
        If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only
        missing or closed indices.

        This behavior applies even if the request targets other open indices.
      schema:
        type: boolean
      style: form
    indices.validate_query::query.analyze_wildcard:
      in: query
      name: analyze_wildcard
      description: If `true`, wildcard and prefix queries are analyzed.
      schema:
        type: boolean
      style: form
    indices.validate_query::query.analyzer:
      in: query
      name: analyzer
      description: |-
        Analyzer to use for the query string.
        This parameter can only be used when the `q` query string parameter is specified.
      schema:
        type: string
      style: form
    indices.validate_query::query.default_operator:
      in: query
      name: default_operator
      description: 'The default operator for query string query: `AND` or `OR`.'
      schema:
        $ref: '../schemas/_common.query_dsl.yaml#/components/schemas/Operator'
      style: form
    indices.validate_query::query.df:
      in: query
      name: df
      description: |-
        Field to use as default where no field prefix is given in the query string.
        This parameter can only be used when the `q` query string parameter is specified.
      schema:
        type: string
      style: form
    indices.validate_query::query.expand_wildcards:
      in: query
      name: expand_wildcards
      description: >-
        Type of index that wildcard patterns can match.

        If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.

        Supports comma-separated values, such as `open,hidden`.

        Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
      schema:
        $ref: '../schemas/_common.yaml#/components/schemas/ExpandWildcards'
      style: form
    indices.validate_query::query.explain:
      in: query
      name: explain
      description: If `true`, the response returns detailed information if an error has occurred.
      schema:
        type: boolean
      style: form
    indices.validate_query::query.ignore_unavailable:
      in: query
      name: ignore_unavailable
      description: If `false`, the request returns an error if it targets a missing or closed index.
      schema:
        type: boolean
      style: form
    indices.validate_query::query.lenient:
      in: query
      name: lenient
      description: If `true`, format-based query failures (such as providing text to a numeric field) in the query string will
        be ignored.
      schema:
        type: boolean
      style: form
    indices.validate_query::query.q:
      in: query
      name: q
      description: Query in the Lucene query string syntax.
      schema:
        type: string
      style: form
    indices.validate_query::query.rewrite:
      in: query
      name: rewrite
      description: If `true`, returns a more detailed explanation showing the actual Lucene query that will be executed.
      schema:
        type: boolean
      style: form
