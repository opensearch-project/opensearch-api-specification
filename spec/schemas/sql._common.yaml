openapi: 3.1.0
info:
  title: Schemas of sql._common category.
  description: Schemas of sql._common category.
  version: 1.0.0
paths: {}
components:
  schemas:
    SqlQuery: # Can we name it just Query?
      type: object
      properties:
        query:
          type: string
        filter:
          type: object
        fetch_size:
          type: integer
    SqlResponse:
      type: object
      properties:
        schema:
          type: array
          items:
            type: object
        data_rows:
          type: array
          items:  # this is 2d array. Is it normal to write like this? Or perhaps we need to define DataRows structure at the bottom?
            type: array
            items:
              type: object
        total:
          type: integer
        size:
          type: integer
        status:
          type: string
    # DataRows:
    #   type: array
    #   items: object
        