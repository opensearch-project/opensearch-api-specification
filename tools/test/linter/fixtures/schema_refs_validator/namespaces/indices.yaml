components:
  parameters:
    animals::query.mammal:
      name: mammal
      in: query
      required: true
      schema:
        $ref: '../schemas/animals.yaml#/components/schemas/Mammal'
    vehicles::query.car:
      name: car
      in: query
      required: true
      schema:
        $ref: '../schemas/vehicles.yaml#/components/schemas/Car'