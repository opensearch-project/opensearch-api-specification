$schema: ../../../../../json_schemas/test_story.schema.yaml

description: Test max aggregation with value_type parameter for script outputs.
warnings:
  invalid-path-detected: false
prologues:
  - path: /_bulk
    method: POST
    parameters:
      refresh: true
    request:
      content_type: application/x-ndjson
      payload:
        - {create: {_index: products}}
        - {price: 100.5, category: electronics}
        - {create: {_index: products}}
        - {price: 50.25, category: books}
        - {create: {_index: products}}
        - {price: 75, category: electronics}
chapters:
  - synopsis: Aggregate with max using value_type double on script output.
    path: /{index}/_search
    parameters:
      index: products
    method: POST
    request:
      payload:
        size: 0
        aggs:
          max_price:
            max:
              script:
                source: doc['price'].value
              value_type: double
    response:
      status: 200
      payload:
        aggregations:
          max_price:
            value: 100.5

  - synopsis: Aggregate with max using value_type long on script output.
    path: /{index}/_search
    parameters:
      index: products
    method: POST
    request:
      payload:
        size: 0
        aggs:
          max_price:
            max:
              script:
                source: doc['price'].value
              value_type: long
    response:
      status: 200
      payload:
        aggregations:
          max_price:
            value: 100.5

  - synopsis: Aggregate with max using value_type integer on script output.
    path: /{index}/_search
    parameters:
      index: products
    method: POST
    request:
      payload:
        size: 0
        aggs:
          max_price:
            max:
              script:
                source: doc['price'].value
              value_type: integer
    response:
      status: 200
      payload:
        aggregations:
          max_price:
            value: 100.5

  - synopsis: Aggregate with max using value_type byte on script output.
    path: /{index}/_search
    parameters:
      index: products
    method: POST
    request:
      payload:
        size: 0
        aggs:
          max_price:
            max:
              script:
                source: doc['price'].value
              value_type: byte
    response:
      status: 200
      payload:
        aggregations:
          max_price:
            value: 100.5

  - synopsis: Aggregate with max using value_type short on script output.
    path: /{index}/_search
    parameters:
      index: products
    method: POST
    request:
      payload:
        size: 0
        aggs:
          max_price:
            max:
              script:
                source: doc['price'].value
              value_type: short
    response:
      status: 200
      payload:
        aggregations:
          max_price:
            value: 100.5

  - synopsis: Aggregate with max using value_type unsigned_long on script output.
    version: '>2.0.0'
    path: /{index}/_search
    parameters:
      index: products
    method: POST
    request:
      payload:
        size: 0
        aggs:
          max_price:
            max:
              script:
                source: doc['price'].value
              value_type: unsigned_long
    response:
      status: 200
      payload:
        aggregations:
          max_price:
            value: 100.5

  - synopsis: Aggregate with max using value_type float on script output.
    path: /{index}/_search
    parameters:
      index: products
    method: POST
    request:
      payload:
        size: 0
        aggs:
          max_price:
            max:
              script:
                source: doc['price'].value
              value_type: float
    response:
      status: 200
      payload:
        aggregations:
          max_price:
            value: 100.5
epilogues:
  - path: /products
    method: DELETE
    status: [200, 404]
