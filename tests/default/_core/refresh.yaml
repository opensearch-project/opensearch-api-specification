$schema: ../../../json_schemas/test_story.schema.yaml

description: Test the _refresh endpoint to ensure basic refresh functionality.

chapters:
  - synopsis: Refresh all indices in the cluster.
    path: /_refresh
    method: GET
    response:
      status: 200