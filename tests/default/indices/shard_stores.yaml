$schema: ../../../json_schemas/test_story.schema.yaml

description: Test the _shard_stores endpoint to retrieve information about shard copies.

chapters:
  - synopsis: Retrieve information about shard copies.
    path: /_shard_stores
    method: GET
    response:
      status: 200