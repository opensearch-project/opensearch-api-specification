$schema: ../../../../../json_schemas/test_story.schema.yaml

description: Test retrieving scheduled job stats from specific nodes.

chapters:
  - synopsis: Get all scheduled job stats from the local node.
    path: /_plugins/_alerting/{node_id}/stats/
    method: GET
    parameters:
      node_id: _local
    response:
      status: 200
