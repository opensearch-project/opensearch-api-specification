$schema: ../../../../../../json_schemas/test_story.schema.yaml

description: Test retrieving specific scheduled job stats from specific nodes.

chapters:
  - synopsis: Get the job scheduling metrics of the local node.
    path: /_plugins/_alerting/{node_id}/stats/{stat}
    method: GET
    parameters:
      node_id: _local
      stat: job_scheduling_metrics
    response:
      status: 200
