/replaced/adopting/{a}/something/{b}:
  superseded_by: /adopt/{animal}/dockets/{docket}
  operations:
    - GET
    - DELETE
/something/else:
  superseded_by: /not/here
  operations:
    - POST
    - PUT